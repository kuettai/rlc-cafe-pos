{
  "name": "cafepos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "npx serve frontend -p 3000",
    "build:backend": "cd backend && npm run build",
    "deploy:backend": "cd infra && npx cdk deploy --require-approval never",
    "deploy:frontend": "git add frontend && git commit -m \"Update frontend\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.0",
    "serve": "^14.2.6"
  }
}
