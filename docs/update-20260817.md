# Update — 2026-08-17

Staff ordering link (T2). A customer-facing link `?code=<CODE>` that lets a staff
member order at the staff price without queueing for the cashier to select the
class for them — drinks at the staff rate (RM5), food at full price.

## Context

Staff and pastors could only get their price through a **cashier-created walk-up
order**: the volunteer had to build the cart at the counter and pick the discount
class. On a busy Sunday that puts the slowest kind of order (someone reading the
menu at the till) in front of the queue.

T2 lets staff self-serve. The requirement that shaped the whole design: the
customer may **request** the staff price, but only the cashier may **grant** it.

## Analysis

Two decisions carried the design.

**1. A staff link is not a pre-order link.** The obvious shortcut was to reuse
`PREORDER_CODE#`. The two disagree on every axis
(`backend/src/routes/staffcode.ts:9-15`):

| | Ministry pre-order | Staff link |
|---|---|---|
| price | free, RM0 | RM5 drinks, food full price |
| status on create | PREPARING | PENDING (needs approval) |
| café-open check | bypassed | **applies** |
| food | drinks only | allowed |
| expiry | same-day ISO string | numeric TTL, plus a date gate |

So a staff order is an **ordinary** customer order — ordinary PENDING status,
ordinary numeric `expiresAt` TTL, ordinary food reservation, ordinary
PENDING→PREPARING approve — and only the pricing class differs. No new statuses,
no new transitions, no new arithmetic.

**2. A self-applied price would have stuck even when the cashier declined.** The
order has to be stored already priced at RM5 so the customer sees what they will
pay. But `repriceStoredItems` treats the stored net as the **incumbent
candidate** and only ever charges the cheaper of the two candidates
(`backend/src/lib/pricing.ts:329-346`). Approve an untouched staff-link order
without a `STAFF` selection and:

- the RM5 survives, because it is cheaper than gross; and
- it comes back out labelled `CELEBRATION`, because the incumbent net is below
  gross (`appliedRule = incumbentNet < gross ? 'CELEBRATION' : 'NONE'`).

That is a discount nobody authorised, attributed to the wrong rule, with nobody
accountable in `approvedBy` — and invisible in the discount report. This is now
recorded as a reviewable invariant (`.kiro/skills/invariants/SKILL.md`, Money):
*a customer-requested discount class must never survive approve unless the
cashier explicitly selected it.*

The fix had to restore the **no-class net**, not gross: declining a staff price
must not also throw away a legitimate celebration discount. Hence a new stored
item field rather than reusing `grossUnitPrice`.

## Changes

### Backend

- **`backend/src/routes/staffcode.ts` (new)** — staff-code storage, validation
  and the admin single-entry upsert.
  - `GET /api/staff-code/validate?code=<CODE>` — public, mirrors
    `validatePreorderCode`'s response shape so `createOrder` reads the same
    either way. `200 {valid:true, code, label}` /
    `400 {valid:false, reason:'invalid'|'not_yet'|'expired'}`.
  - `GET|PUT /api/admin/staff-code` — ADMIN. `PUT` is an upsert that sweeps
    every other `STAFF_CODE#` record afterwards, so exactly one survives; there
    is no create/delete pair to get wrong. `createdAt`/`createdBy` are preserved
    when the code itself is unchanged.
  - Code alphabet `ABCDEFGHJKMNPQRSTUVWXYZ23456789` (no 0/O/1/I/L — the code is
    typed by hand off a printed link or a WhatsApp message), 3–16 chars,
    uppercased on **both** write and lookup so the two always agree.
  - `startDate`/`endDate` are `YYYY-MM-DD`, **inclusive at both ends**, empty
    string = unbounded, compared against today in **Malaysia time (UTC+8)** via
    `malaysiaToday()`. The gate is a wall-clock decision made in the café: a code
    ending "today" stays valid until local midnight.
- **`backend/src/index.ts`** — `/api/staff-code` registered in the public block
  next to `/api/preorder`; `/api/admin/staff-code` registered above the generic
  `/api/admin` catch-all with an `ADMIN` role check.
- **`backend/src/routes/orders.ts`** — `POST /api/orders` accepts an optional
  `staffCode`. Supplying both `staffCode` and `preorderCode` is a `400` (the two
  contradict each other on price, status, café-open and food, so the request is
  refused rather than silently ranked). The staff branch sits inside the
  non-pre-order path, so the café-open check still applies. A valid code sets
  `requestedClass = 'STAFF'`, and the order is written with `staffCode` and
  `customerClass: 'STAFF'` — the first time `customerClass` is set at **create**
  time rather than by the cashier at approve. Audit action `CREATE_STAFF`.
- **`backend/src/lib/pricing.ts`** — two additions, no new arithmetic:
  - `revertRequestedClassPricing(items)` — a lookup, not a calculation:
    `unitPrice = baseUnitPrice ?? grossUnitPrice ?? unitPrice`. The
    `grossUnitPrice`/`unitPrice` fallbacks cover records predating the field.
  - `toOrderItem(line, opts?)` takes an optional `{ baseUnitPrice }`. Omitted,
    the returned shape is byte-for-byte what it has always been, so existing
    callers are unaffected.
  - The staff price itself is the unchanged `priceLine(..., 'STAFF')` /
    `STAFF_DRINK_PRICE` path, and `baseUnitPrice` is the same `priceLine` called
    with a null class.
- **`backend/src/routes/pos.ts`** (`approveOrder`) — when `order.staffCode` is
  present and the cashier did **not** pass `discountType: 'STAFF'`, items go
  through `revertRequestedClassPricing` **before** `repriceStoredItems`.
  `approvedBy` is recorded either way. The APPROVE audit line now carries
  `staffCode` and `staffPriceGranted`.
- **`backend/tests/staff-code.test.ts` (new)**, `backend/tests/router.test.ts` —
  fully mocked, offline. Pins both load-bearing properties: the staff link keeps
  every ordinary-order rule, and the requested price reverts unless confirmed.

### Data

New settings-table record type: `PK = STAFF_CODE#<CODE>`, `SK = META`, attributes
`{ code, label, isActive, startDate, endDate, createdAt, createdBy, updatedAt }`.
It carries **no `expiresAt`**, so the settings-table TTL cannot reach it — the
link is long-lived and date-gated instead.

New order attributes: `staffCode` (string) and `customerClass: 'STAFF'` at create
time. New order-**item** attribute `baseUnitPrice` (number), written only on
staff-link orders.

### Frontend

- **`frontend/js/admin-stafflink.js` (new)** — Admin > 🎫 Staff Link tab, the
  single-entry editor. Added to the `SHELL` array in `frontend/sw.js` and
  script-tagged in `frontend/admin.html`.
- **`frontend/js/app.js`** — unknown `?code=` values are tried as a pre-order
  code first (unchanged behaviour) and then as a staff code. In staff mode the
  customer sees a banner stating the deal plainly — drinks at the staff price,
  food at full price, *"the cashier confirms staff price when you pay"* — and the
  cart totals are previewed through `CafePricing` (display only; the backend
  number always wins). Submits `staffCode` with the order.
- **`frontend/js/pos.js`** — a staff-link order is tagged "🎫 staff price
  requested" in the queue and in the order detail, and approving one opens a
  confirmation modal first: *"Yes, staff price — RM x"* / *"Not staff — charge
  full price (RM y)"*. Dismissing the modal leaves the order PENDING and
  untouched, so a staff-price request cannot be approved without the cashier
  being asked.
- `frontend/js/admin.js`, `frontend/admin.html`, `frontend/css/admin.css`,
  `frontend/css/style.css` — tab wiring and styling.

### Documentation (this session)

- `.kiro/skills/api-reference/SKILL.md` — the three new endpoints, `staffCode` on
  `POST /api/orders`.
- `.kiro/skills/db-schemas/SKILL.md` — Record Type 14 (Staff Codes), the new
  order attributes, `baseUnitPrice` in the item shape.
- `.kiro/skills/pricing-rules/SKILL.md` — who may select a class (cashier, plus
  the one customer-requested case), the revert step,
  `revertRequestedClassPricing` / `baseUnitPrice` in the API surface.
- `.kiro/skills/order-lifecycle/SKILL.md` — the approve transition also reprices,
  and the staff-code revert that runs in front of it.
- `.kiro/skills/invariants/SKILL.md` — the new invariant (Money section).
- `.kiro/skills/test-suites/SKILL.md` — `staff-code.test.ts` in the offline table.
- `.kiro/steering/project.md` — `staffcode` route, `admin-stafflink` module.
- `docs/requirements.md` §6 — the pricing table said staff self-orders get no
  discount; that is now wrong, so the row was split into "staff link
  (self-order, requested)" and "self-order without a staff link".
- `docs/feature-history.md` — "Staff/Pastor (walk-up only)" in Important Context
  corrected the same way.

## Accepted trade-off — residual security risk

Recorded here for the PR write-up. **The café accepted this knowingly; it is not
a blocker and was not treated as one.**

The staff code is permanent and guessable. It has no per-person identity, no
one-time token and no rate limit, so anyone who holds or guesses the link can
**request** the staff price — the code alphabet is deliberately small and
human-typeable, which helps a guesser as much as it helps a staff member.

The control is not the secret, it is the cashier: the price is stored as a
request, the POS forces a confirmation prompt before approve, and declining
reverts to celebration-or-full. The honest weakness is that on a busy Sunday a
volunteer may tap the primary button through without reading it.

What makes that survivable is that it is **auditable after the fact**: every
APPROVE audit line on a staff-link order records `staffCode` and
`staffPriceGranted`, so the grant rate can be reviewed and a leaked code can be
rotated or deactivated from Admin > Staff Link. Date gating (`startDate` /
`endDate`) is available if the café later wants the link to be seasonal.

## Verification

Documentation-only session; no application code was touched here. Run against
the working tree as it stands:

```
npm run version:check
  → ✓ All 6 version markers agree (v1.70.2), SHELL complete.
    (frontend/js/admin-stafflink.js is in the SHELL array)

cd backend && npx tsc
  → clean, no output

cd backend && npx jest --testPathIgnorePatterns integration
  → 12 suites passed, 187 tests passed
    (includes the new tests/staff-code.test.ts; the previously noted
     expiry.test.ts failures no longer reproduce)
```

The live-API `backend/tests/integration.test.ts` was **not** run — it writes to
the production café and needs it OPEN.

## Open items

Not defects.

1. **Version bump still pending.** The working tree is at v1.70.2 and the staff
   link is unreleased. The bump (a minor — this is a feature) and its
   `frontend/changelog.json` entry are being written centrally, together, so the
   top changelog version and the six markers stay in agreement.
2. **The staff-code routes are not deployed yet.** `GET /api/staff-code/validate`
   and `GET|PUT /api/admin/staff-code` do not exist in production, so the
   frontend was verified against **stubs** for those three paths. Deploy order
   matters here: `npm run deploy:backend` must land before the frontend ships, or
   the staff link 404s for real customers.
3. **Variant pickers still show "Iced (+RM1)" in staff mode.** Cosmetic only —
   the STAFF rule absorbs variant modifiers, so the drink is RM5 either way and
   the cart total shown is correct. Undecided whether to hide the modifiers,
   grey them out, or leave them; worth a decision from the café before this goes
   in front of staff.

---

# T4 — Editable ministry pre-orders (PENDING + a cashier release)

Separate work item, same working tree, same unreleased version. Pre-orders are now
**created PENDING** so the customer can still edit them, and the cashier's
PENDING → PREPARING approve — presented as **"release to barista"** — is the lock.

> Supersedes two rows of T2's comparison table above: a ministry pre-order's
> *status on create* is now **PENDING**, not PREPARING, and its expiry is an ISO
> string that can sit days out, not same-day. T2's conclusion is unaffected — a
> staff link is still not a pre-order link, and every other row still holds.

## Context — the actual harm

A pre-order was created `status: 'PREPARING'`. `modifyOrder` only edits a **PENDING**
order (`ConditionExpression: '#s = :pending'`), so a ministry pre-order was
**permanently uneditable from the moment it was placed** — days before the service
it was for. The workaround volunteers had found was to **delete the order in the
POS and ask the customer to order again**, which produced duplicate orders in the
queue and in the day's figures.

## Analysis and design

**PENDING is the whole fix.** Creating the order PENDING reuses the existing
customer *Edit Order* flow and its race-safe `#s = :pending` gate unchanged — no
new status, no new gate, no second edit path to keep in step. What was lost by
moving out of PREPARING was the implicit "the cashier owns it now" signal, so that
became explicit: the ordinary approve becomes the release, and the customer's edit
window closes exactly when the barista starts the drink.

Everything else in T4 follows from pre-orders now living in a bucket they never
lived in before, and each of these was a real hole rather than a precaution:

- **They must not be swept as stale PENDING orders.** The 1-hour cron already
  skipped `isPreOrder` defensively; that skip is now load-bearing, and is what
  lets a Wednesday pre-order survive to Sunday.
- **Closing the café must not expire them** — see below; this one was live.
- **They must still be expirable.** `expirePreOrders()` now sweeps PENDING as well
  as PREPARING/READY. Without that, an unreleased pre-order would be immortal:
  the hourly sweep skips it and `closeCafe` skips it.
- **They must not be billed at release.** `parseCustomerClass` refuses `'PREORDER'`
  from a request body and the cashier's dropdown has no such entry, so the class is
  forced from the stored `isPreOrder` flag. With a null class `repriceStoredItems`
  would keep the stored (full) `unitPrice` as the cheapest incumbent and charge the
  ministry the full gross with `discountType: 'NONE'`.
- **The edit path had to be closed as a bypass.** `modifyOrder` enforced *none* of
  the three pre-order restrictions `createOrder` enforces — drinks-only,
  `eligibleItems`, `excludedOptions`. Since a pre-order zeroes the whole gross,
  that was uncapped cost to the café: place a compliant pre-order, then edit it
  into food or into an excluded paid option. Both paths now call one helper,
  `preorderItemRejection()`. **Generalised as a new invariant:** any restriction
  enforced at create must be re-enforced on edit, or the edit endpoint *is* the
  bypass.
- **`expiresAt` stays an ISO string, and the release preserves it.** The house rule
  is "every transition out of PENDING must `REMOVE expiresAt`", and it exists
  because a *numeric* `expiresAt` is a live DynamoDB TTL. A pre-order's value is
  the ISO `serviceEndTime`: TTL only acts on numeric attributes, so it is inert,
  and it is the only input to `expirePreOrders()`. The release therefore appends
  `REMOVE expiresAt` **only when the value is not a string** — preserving the ISO
  one, stripping a stray numeric one (which would be a real armed TTL on an order
  meant to live for days).

## The bulk release

`PUT /api/pos/preorders/release-all` (CASHIER/ADMIN, no body — the acting cashier
comes from the JWT and is written to `approvedBy`) returns
`{ released, skipped, total }`. It calls the **same** helper as the per-order
release, so "release four individually" and "release all four" cannot diverge on
the guard, the pricing, the audit line, the push or the ingredient deduction.

Two safety filters, both load-bearing:

1. **`isPreOrder === true`.** The route walks *every* PENDING order, and that bucket
   is full of ordinary **unpaid** customer orders waiting for the cashier's payment
   check. Lose this filter and the route mass-approves them — unpaid orders marked
   paid and sent to the barista.
2. **Service-end date is today in Malaysia time.** A link can stay open across more
   than one service, so PENDING can hold orders for a *later* date; releasing those
   would close the edit window for customers who ordered for next week. An order
   with no usable ISO `expiresAt` is skipped and logged, never guessed at.

It also paginates on `LastEvaluatedKey`. Silent 1MB truncation in a *mutating*
batch would tell the cashier "released 40" — so they would reasonably believe the
queue was empty — while real pre-orders stayed stranded in PENDING. A per-order
`ConditionalCheckFailedException` counts as *skipped* and never fails the batch, so
`released + skipped === total` always holds.

There is deliberately **no scheduled auto-release**: a time-based release would
close the customer's edit window behind their back.

## Changes

### Backend

- `backend/src/routes/orders.ts` — pre-orders created **PENDING** with
  `customerClass: 'PREORDER'`; totals now come from `pricing.ts` instead of
  hardcoded zeros (which also stops `linkOrderToCustomer` recording a pre-order's
  full gross as customer lifetime spend); `preorderItemRejection()` shared by
  create and edit; `modifyOrder` re-enforces the three restrictions and owns the
  notes prefix; `getOrder` returns pre-order context.
- `backend/src/lib/pricing.ts` — new `'PREORDER'` customer class (DRINK → RM0,
  FOOD untouched, no new arithmetic), mapped unconditionally to
  `discountType: 'MINISTRY_PREORDER'`; `DiscountType` `Exclude`s `'PREORDER'` so
  forgetting the mapping is a compile error; `parseCustomerClass` still refuses it.
- `backend/src/routes/pos.ts` — `releasePreOrderToPreparing()` (shared),
  `releaseAllPreOrders()`, `approveOrder` delegating for pre-orders, `closeCafe`
  skipping them, and the shift summary picking up pre-orders created before today.
- `backend/src/expiry.ts` — `expirePreOrders()` sweeps PENDING too, and self-heals
  an order that has lost `expiresAt` from the link's `serviceEndTime` (backfilled
  as an ISO string, one code lookup per code per sweep, status-guarded).
- `backend/src/routes/preorder.ts` — `getPreorderCode()`: a plain lookup with **no**
  time-window validation, for reading restrictions on an order that already exists.
  Using `validatePreorderCode` there would refuse a legitimate edit of a
  still-PENDING order after the link's ordering window closed.
- `backend/src/routes/receipt.ts` — receipt upload refused on a pre-order.
- `backend/src/routes/admin.ts` — the daily report gained a third bucket (PENDING
  pre-orders, `isPreOrder`-filtered) so they stay visible in Today's Stats.
- `backend/tests/preorder-pending.test.ts` (new, fully mocked/offline),
  `backend/tests/router.test.ts`.

### Frontend

- `frontend/js/pos.js` — pre-order ribbon and purple card treatment, "Release to
  barista" wording, the bulk release button (only for pre-orders due today), and
  no urgent chime / no receipt-mismatch badge on a free order.
- `frontend/js/track.js` — pre-order view (free, no payment step, collection time
  shown separately from the customer's own note) and the pre-order edit flow.
- `frontend/js/variants.js` — the option-exclusion filter moved here from `app.js`
  as `RLCVariants.applyOptionExclusions()`, because two surfaces now need it and
  variant selection is a single source of truth.
- `frontend/js/app.js` — now calls that shared helper.
- `frontend/css/style.css` — pre-order card/ribbon styling.

No new `frontend/js` or `frontend/css` file, so nothing to add to the `sw.js`
`SHELL` array.

## Worth a volunteer's / maintainer's attention

1. **Closing the café previously expired every outstanding pre-order.** `closeCafe`
   queries PENDING orders **unbounded by date** and had no `isPreOrder` guard —
   it never needed one while pre-orders were born PREPARING. Under T4, closing the
   café once would have EXPIRED every pre-order for every future service. Fixed:
   `closeCafe` now skips them.
2. **Pre-order drinks now deduct ingredient stock.** `deductIngredients()` runs only
   on the approve/release path, which pre-orders never reached before (they were
   created PREPARING). Ingredient usage and stock numbers will therefore include
   pre-order drinks from now on — an accuracy improvement, but expect the figures
   to move.
3. **A live defect found and fixed en route:** in `frontend/js/track.js`,
   `isEditing` was set but **never read**, so every 7s poll re-rendered the page
   and silently threw away a customer's in-progress edit. It affects all customer
   edits, not just pre-orders. The poll now returns early while editing, and drops
   edit mode with an explanation if the order leaves PENDING (the backend would
   answer 409 anyway).

## Verification

Documentation-and-notes pass. Run against the working tree as it stands:

```
npm run version:check
  → ✓ All 6 version markers agree (v1.70.2), SHELL complete.
```

`backend/tests/preorder-pending.test.ts` is fully mocked and offline. The live-API
`backend/tests/integration.test.ts` and the Playwright journeys were **not** run —
they write to the production café.

## Audit findings — T4 (follow-up pass)

An independent auditor ran 30 source mutations against the T4 tests. Most of the
suite has real teeth: the bulk-release safety filters, free-price preservation,
edit-path enforcement, ISO `expiresAt` handling, `closeCafe`, the undo round trip,
the notes prefix and the router all failed under mutation as they should. The
offline test count went **254 → 267** across two follow-up passes (14 suites).

Two findings, both new classes of bug rather than new behaviour:

1. **The most serious: the 1-hour PENDING sweep's pre-order skip was never
   exercised.** Deleting `if (order.isPreOrder === true) continue;` from
   `backend/src/expiry.ts` left all 254 tests passing. No fixture in the repo had
   ever put a row in front of *that* query — the suites staged `{ Items: [] }` for
   it, so every assertion about the sweep's behaviour was vacuous, and a
   neighbouring test appeared to cover it while staging into a different query in
   the same handler. Had that line been lost, the cron would EXPIRE every
   pre-order within an hour of creation and the `REMOVE expiresAt` on the way out
   would destroy the service-end time too: the feature dies silently, on a
   schedule, days before anyone would notice. **Now covered.**
2. **A dead branch in `approveOrder` duplicated the ISO-`expiresAt` rule.**
   `backend/src/routes/pos.ts` returns early for pre-orders and delegates to
   `releasePreOrderToPreparing()`, but the money path below still carried a full
   pre-order case — an `isPreOrder` flag always false, a `keepIsoExpiry` always
   false, and the better-written copy of the preservation comment. Unreachable,
   proven by inserting a `throw` and watching the suite stay green. The hazard was
   that the next agent could edit the no-op, verify by reading, and ship nothing.
   **Verified removed in this working tree**; what survives at the superseded site
   is a one-line pointer ("do not add a pre-order case here").

Coverage added: **`backend/tests/preorder-pending-gaps.test.ts`** (new, 13 tests,
fully mocked/offline, no credentials, writes nothing to production) — the 1-hour
sweep's pre-order skip; the `409` on a stale status for the single-order release
path; `getOrder`'s five pre-order response fields; the closed-window link still
supplying its restrictions on edit; and the admin daily-report pre-order bucket in
both directions, whose only previous coverage was the live integration suite.

Still open: **`backend/tests/pricing.test.ts` contains no `PREORDER` cases** —
confirmed, zero occurrences in the tree. The behaviour is covered elsewhere
(`preorder-pending.test.ts`), but `pricing.test.ts` is the discount *specification*
and a class that zeroes every DRINK line belongs in it. Being closed separately.

### Caveats — limits of this verification, not results

- Every mutation was checked against a **mocked `docClient`**. What was verified is
  the `UpdateExpression` the code builds, **not** that DynamoDB honours it. "An ISO
  string is inert as a DynamoDB TTL" remains an assumption taken from the skill
  docs, not an observation against the live table.
- **`malaysiaToday()` was never exercised across a real UTC/MYT day boundary.** The
  tests compute expected dates with the same `+8h` arithmetic the implementation
  uses (`backend/src/routes/staffcode.ts`), so a shared error in that assumption
  would be invisible to them.

## Not shipped yet — release state

- **NOT version-bumped.** The tree is at **v1.70.2** and stays there; T1 + T2 + T4
  are being bumped together in one later release pass so the six markers and the
  top `changelog.json` entry are written by a single `npm run version:bump`.
- **NOT in `frontend/changelog.json`.** The user-visible line ("edit your ministry
  pre-order until the counter starts it") belongs to that same pass.
- **NOT deployed.** Neither half.
- **Both halves must ship, backend first.** The two deploy paths are independent:
  `npm run deploy:backend` (CDK) and `npm run deploy:frontend` (git push; the Pages
  workflow only fires on a push to `master` touching `frontend/**`). Until the new
  `getOrder` response is deployed, `isPreOrder` is absent and **track.html renders
  a pre-order as an ordinary order with a payment block** — asking the customer to
  pay for a free order. Equally, `PUT /api/pos/preorders/release-all` 404s until the
  backend lands.
- `docs/feature-history.md` has **no sprint entry for T4** — that belongs with the
  version bump, alongside T1/T2, and is listed here so it is not forgotten.

## Documentation updated in this pass

- `.kiro/skills/invariants/SKILL.md` — the pre-order ISO `expiresAt` exception and
  the release that preserves it; `parseCustomerClass` must refuse a system-only
  class; a new **Create / edit parity** section (new class of bug); collection-route
  dispatch; bulk mutating routes; backend-owned operational fields.
- `.kiro/skills/order-lifecycle/SKILL.md` — pre-orders enter PENDING, the release
  transitions (single + bulk), who may expire a pre-order, `closeCafe` skipping
  them, the undo recovery path, ingredient deduction at release.
- `.kiro/skills/pricing-rules/SKILL.md` — the system-only `PREORDER` class, the
  `MINISTRY_PREORDER` mapping, item `unitPrice` staying full until release.
- `.kiro/skills/db-schemas/SKILL.md` — `isPreOrder`, `customerClass: 'PREORDER'`,
  `expiresAt` as number-or-ISO-string, `notes` as the only record of the collection
  time, plus `grossAmount` / `modifiedAt` rows that were missing.
- `.kiro/skills/api-reference/SKILL.md` — `PUT /api/pos/preorders/release-all`, the
  extra `GET /api/orders/{id}` fields, the enforcing `PUT /api/orders/{id}`, the
  receipt-upload refusal, pre-orders created PENDING.
- `.kiro/skills/test-suites/SKILL.md` — `preorder-pending.test.ts` in the offline
  table (writes nothing to production, no new test-data marker).
- `docs/requirements.md` §8 and `docs/architecture.md` §6.4/§6.5 — the 1-hour
  PENDING expiry and the end-of-day expire-all no longer apply to pre-orders.
- `.kiro/steering/project.md` — **unchanged, verified accurate:** T4 added no
  `backend/src/routes/*.ts`, no `backend/src/lib/*.ts` and no `frontend/js/*.js`.

Added by the audit follow-up pass (see **Audit findings** above):

- `.kiro/skills/invariants/SKILL.md` — a new **Test teeth** section (a guard in a
  loop over query results is untested unless a fixture reaches it; each query of a
  multi-query handler needs its own fixture; a test asserting on the object it
  built cannot fail; a fixture too sparse to isolate the guard has no teeth), plus
  two items in **do not duplicate**: delete the code an early return supersedes, in
  the same change, and never let a rule survive in two copies. `description`
  frontmatter updated in both `.kiro` and `.claude/skills/invariants/SKILL.md` so
  the skill is found for test-review work.
- `.kiro/skills/test-suites/SKILL.md` — `preorder-pending-gaps.test.ts` in the
  offline table, and a "Writing tests" pointer to **Test teeth**.

## Open items — T4 (not fixed, deliberately)

1. **The "cancelled by customer" toast misfires on a cashier-rejected order.**
   `frontend/js/pos.js` infers a customer cancellation from a PENDING order
   disappearing between two polls, so a reject the cashier just performed comes
   back as *"X's order was cancelled by customer"*. **Pre-existing**, unrelated to
   pre-orders, and now slightly more visible because the pre-order queue is
   busier.
2. **Pre-orders still zero out paid options** — a ministry pre-order for an Iced
   drink gets the +RM1 modifier free, because the `PREORDER` class prices the whole
   DRINK line at RM0 (exactly as PASTOR and NEWCOMER do). Raised previously and
   **explicitly out of scope**; it is a pricing-policy decision for the café, not a
   bug in this change.
3. **POS modals render bottom-flush at ≤768px.** The release-all confirmation
   inherits `.pos-modal-overlay{align-items:flex-end}` from the ≤768px block in
   `frontend/css/style.css`. **Pre-existing house style** across every POS dialog;
   changing it is a global design decision, not a T4 fix.

---

# T1 — Checklist reordering (Open / Close / Handover)

The third item in this release, with no section of its own above. **Frontend
only** — no backend change, no schema change, no migration.

Item order is purely **array position**; there is no `sortOrder` field anywhere in
the checklist config. Admin renders `items.map((item, i))` and the POS renders the
same arrays, and the save is a whole-array `PutCommand`, so a reorder is a
`splice` plus the existing `rerender()` and it round-trips unchanged.

- `frontend/js/admin-checklist.js` — a ⠿ drag handle and ▲▼ buttons per row.
  Dragging uses **pointer events, not HTML5 drag**, which is unreliable on the
  counter iPad, and the gesture starts on the handle only so selecting text in a
  row's label input never begins a drag. ▲ is disabled on the first row, ▼ on the
  last. `${i+1}.` renumbers live. Reordering is **within a phase only** — each
  phase has its own container element, so a close item cannot land in the open
  flow. Nothing persists until **Save Checklist**, matching how label / type /
  enabled edits already behave. After an arrow press focus is restored to the same
  arrow on the row that moved, so a volunteer can tap ▲ repeatedly.
- `frontend/css/admin.css` — row, handle and button styling.

No new `frontend/js` or `frontend/css` file, so nothing to add to the `sw.js`
`SHELL` array.

**Documentation:** `.kiro/skills/db-schemas/SKILL.md` Record Type 2 said only
"stores the list of checklist items". It now records that array position **is** the
display order and that sorting or rebuilding those arrays destroys an ordering a
volunteer set deliberately — the ordering became load-bearing with this change.
Nothing else in the skill/doc table applies: T1 added no endpoint, attribute,
status transition, pricing rule, route module or page module.

---

# Release pass — v1.71.0

T1 + T2 + T4 shipped as one release. Previous sections were written while the tree
was still at v1.70.2 and stated the bump was pending; that is now done.

## Version

**v1.70.2 → v1.71.0** (minor — two new features plus a behaviour change), via a
single `npm run version:bump -- minor`, so all six markers and the top
`changelog.json` entry were written together. No marker was hand-edited.

`frontend/changelog.json` v1.71.0 carries five volunteer-facing lines: the staff
link and its cashier confirmation; editable ministry pre-orders; the POS Release
All button; Edit Order on the confirmation screen; and checklist reordering.

`docs/feature-history.md` gained a **2026-08-17 Sprint — v1.71.0** entry covering
all three tasks, the two fixes found en route (café close expiring pre-orders, the
discarded in-progress edit) and the test-count movement. Its "Current Status"
date was stale at 2026-07-19 and now reads 2026-08-17.

## Stale reference fixed

`backend/tests/integration.test.ts:42` pointed at `scripts/cleanup-test-orders.mjs`,
which **does not exist** — the real script is `scripts/cleanup-test-data.mjs`. That
comment is the instruction someone reads immediately after knowingly writing real
orders to production, so a wrong filename there costs exactly the cleanup it exists
to prompt. Corrected; it was the only occurrence in the tree.

## Live integration run — production was written to

`backend/tests/integration.test.ts` **was run against production today** and passed
**19 passed / 2 skipped**. Recorded because this suite is not a read-only check —
it mutates the live café, and this run did:

- **deducted real ingredient stock** on the order it approved;
- **reset food quantities to 0** when it closed the café;
- **delivered an end-of-day summary email to the admin** — a real report the café
  will see in its inbox for a test order.

**`scripts/reset-food-reserved.mjs` was NOT run, and does not need to be.** An
earlier draft of this note claimed `foodReserved` drift might remain; that was
wrong. `closeCafe` (`pos.ts:1082-1087`) scans every FOOD menu item and sets
`foodQuantityToday = 0`, `foodReserved = 0`, `isEnabledToday = false` — a full
wipe, not a reconciliation — so the test close left both counters at a clean
zero. `reset-food-reserved.mjs` exists for a different failure: a stranded
non-zero `foodReserved` with no live order behind it, which makes items look
sold out.

**The real consequence is the wipe itself.** Every FOOD item is now at quantity 0
with `isEnabledToday = false`; verified against the live API, which returns 0
FOOD items and DRINK only. If Sunday's food quantities had been set up in
advance, they are gone. Food quantities must be set again before service — normal
pre-service work, but do not assume the earlier setup survived.

Do **not** re-run the suite as part of verifying this release; it has already been
run once today and each run repeats all three effects.

## Verification (this pass, re-run against the tree as committed)

```
cd backend && npx tsc --noEmit
  → exit 0, no output

cd backend && npx jest --testPathIgnorePatterns integration
  → 14 suites passed, 276 tests passed, 0 failed

npm run version:check
  → ✓ All 6 version markers agree (v1.71.0), SHELL complete.
```

These supersede the counts in the T2 section above (12 suites / 187 tests), which
were correct when written and were overtaken by the T4 and audit passes.
`frontend/js/admin-stafflink.js` is the release's only new frontend asset and was
already in the `sw.js` `SHELL` array before this pass.

## Accepted behaviour changes — expect the numbers to move

Neither is a defect. Both are consequences of pre-orders now passing through the
approve/release path they never reached while they were born PREPARING.

1. **Pre-order drinks now deduct ingredient stock at approve.** `deductIngredients()`
   runs on the approve/release path only, so pre-order drinks were previously
   consumed without ever being counted. Ingredient usage and stock figures are more
   accurate from now on, but they will **step** at this release — a like-for-like
   comparison against earlier weeks will read as a jump in consumption rather than a
   change in method.
2. **A pre-order released early is archived when the café closes.** Once released it
   is an ordinary PREPARING order, so the end-of-day sweep treats it as one. A
   pre-order released before the service it was placed for will therefore be
   archived that evening rather than waiting for its collection date. Accepted: the
   cashier chooses when to release, and the fix for releasing too early is not to
   release too early.

## Follow-ups NOT done in this release

Recorded so they are not lost. None of these block the deploy.

a. **`frontend/css/admin.css:69` animates `margin-left` on `.admin-main`**
   (`transition:margin-left .25s ease`) — a layout property, so every frame
   triggers layout and the transition **janks on the counter iPad**. The proper
   fix is a transform-based push: `.pos-sidebar` already moves itself that way
   (`transform:translateX(-100%)` / `transition:transform`,
   `frontend/css/style.css:2090`). **Deferred** because it restructures the admin
   sidebar that was only just retuned, and doing it inside a release that already
   moves the admin surface would make a visual regression hard to attribute.

   Verified while writing this up, and **wider than the original report**:
   `.pos-main` (`frontend/css/style.css:2129`) has the *identical*
   `transition:margin-left .25s ease`. Only the sidebar element itself is
   transform-based; both main panes animate a layout property. So the POS is not
   the finished reference implementation this note first implied — fixing admin
   properly means fixing the same pattern in two places.

   **DOWNGRADED in v1.75.0, not fixed.** Both `margin-left` transitions are
   unchanged, but the sidebar is now **docked and static at ≥900px**
   (`frontend/css/style.css:2810`, `frontend/css/admin.css:166`), so at the counter
   iPad's landscape width — the one that matters — there is no sliding transition
   left to jank. It remains a real defect below 900px and in portrait.

b. ~~**No stored `collectionTime` attribute on a pre-order.** The collection time
   lives only inside the `notes` prefix, so it cannot be amended without
   delete-and-re-order. **The café decided to keep the existing pre-order logic for
   now**; this is a deliberate deferral, not an oversight.~~

   **ADDRESSED — see "Per-item notes and editable collection times" at the end of
   this file (work done 2026-08-18).** The collection time is now amendable on
   `PUT /api/orders/{id}`, within the link's own `collectionOptions`. Note what
   did *not* change: there is still **no `collectionTime` attribute** — the user
   decided against one — so the time still lives only in the `notes` prefix. Only
   the "cannot be amended" half of this item was resolved.

c. **Pre-orders still zero any paid option** — a ministry pre-order for an Iced
   drink gets the +RM1 modifier free, because the `PREORDER` class prices the whole
   DRINK line at RM0 (as PASTOR and NEWCOMER do). Auditable via `discountOffset`
   but uncapped. A pricing-policy decision for the café, explicitly out of scope.

d. **`closeCafe` and `listOrders` still have un-paginated `Query` calls** — the same
   silent 1MB-truncation exposure that was fixed in the bulk release route.
   Verified: two each, neither passing `ExclusiveStartKey` or reading
   `LastEvaluatedKey` — `backend/src/routes/pos.ts:1011` and `:1045` in `closeCafe`,
   `:226` and `:242` in `listOrders`. In `closeCafe` a truncated page means orders
   that should have been expired or archived are quietly left behind; in
   `listOrders` the cashier sees a short queue and believes it. Today's volumes are
   far below 1MB, which is why this is a follow-up and not a blocker — but the fix
   is the same `LastEvaluatedKey` loop already written for `releaseAllPreOrders`
   (`:537`) and for `sendDailySummaryEmail` (`:1145-1157`, which **does** paginate,
   so the end-of-day email is not exposed).

e. **`value="${item.label}"` is unescaped in the admin row builders** — a label
   containing a double quote closes the attribute early and breaks the markup of
   the row. Reachable by an admin typing `5" cup` into a checklist item. Same-origin
   admin-authored text, so the realistic damage is a mangled form rather than an
   XSS, but it should be escaped.

   Verified, and the fix is smaller than it looks: **an escaper already exists** —
   `mfEsc()` in `frontend/js/admin-menu.js`, applied to option and group names
   (`:238`, `:245`). It is simply not applied to the other free-text `value="…"`
   interpolations: `frontend/js/admin-checklist.js:21` (the T1 row),
   `admin-menu.js:194` and `:197` (menu name, description),
   `admin-ingredients.js:247` (ingredient name). Numeric and date fields in that
   list are not at risk. So this is "reuse the existing helper in four more places",
   not new code — and worth doing as one pass rather than per-feature.

f. **Staff mode still shows "Iced (+RM1)" in the variant picker** though the flat
   RM5 staff rule absorbs modifiers, so the drink is RM5 either way and the cart
   total shown is correct. Cosmetic but misleading — it invites a staff member to
   avoid a choice that costs them nothing. Fix is to strip the modifier price in
   staff mode and add a clause to the staff banner saying options are included.

## Still for the user to run

Nothing is deployed. Both halves are prepared and **backend must land first**:

```
npm run deploy:backend     # CDK → ap-southeast-5, account 956288449190
npm run deploy:frontend     # git push; Actions publishes Pages
```

Until the backend is live, `GET /api/staff-code/validate`, `GET|PUT
/api/admin/staff-code` and `PUT /api/pos/preorders/release-all` 404, and the new
`getOrder` fields are absent — which makes **track.html render a free pre-order as
an ordinary order with a payment block**, asking a customer to pay for a free
order. This release also touches `frontend/**`, so the Pages workflow will fire on
the push; the commit is not backend-only.

---

# Second release pass — v1.72.0

A second release on the same day, on top of the committed-but-unpushed v1.71.0.
Two pieces of work: the Admin Dashboard redesign (frontend only) and the
end-of-day revenue email fix (backend + CDK).

## Version

**v1.71.0 → v1.72.0** (minor — two user-visible features plus a user-visible bug
fix), via a single `npm run version:bump -- minor`. All six markers and the top
`changelog.json` entry were written together; none was hand-edited.
`npm run version:check` reports all six agreeing at v1.72.0 with SHELL complete.

No new `frontend/js` or `frontend/css` file, so the `sw.js` `SHELL` array is
unchanged — `admin-dashboard.js` and `admin.css` were already precached.

`frontend/changelog.json` v1.72.0 carries six volunteer-facing lines: the
restructured dashboard, the three charts, the order-source breakdown, the
date-aware headings, and the two email fixes (that it will arrive again, and that
it will be dated correctly).

## The email root cause

`closeCafe` ended with `sendDailySummaryEmail().catch(() => {})` — started, never
awaited, fired after the handler had already returned its 200. **This was not a
config or logic fault: the summary code and the SMTP settings were correct all
along.** Lambda freezes the execution environment the instant a handler returns, so
that orphaned promise only advanced when the very same sandbox was thawed by some
later request. On busy Sundays it got that luck and the email arrived late but
intact (2026-08-02 at +50s, 2026-08-09 at +4m39s — both completing against a
*later* request id than the close). On the quiet 2026-08-16 the sandbox took two
more requests and was reaped 0.78s after the close, and no email was ever sent.
The `.catch(() => {})` swallowed the evidence, and the caller also discarded
`sendEndOfDaySummary`'s boolean return, so there was no log line either way — which
is why a missing weekly report went unnoticed for a week.

**The 2026-08-16 report is NOT recoverable by this change.** The fix is forward-only:
the next qualifying run will produce the **2026-08-23** summary. Nothing replays a
past date, and the `DAILY_SUMMARY#{date}` marker is keyed to the current MYT date.
The correct 16 August figures are not lost — they are on the **admin Reports page**,
which reads the same orders the email would have summarised. Read them there if the
number is needed.

## Why the fix is in the cron, not an `await` in `closeCafe`

Awaiting it in the handler would put a ~4s third-party SMTP call inside a cashier's
request against a 10s function timeout, on the one gesture that ends the service.
So the summary moved to the expiry cron (`sendDailySummary()` in
`backend/src/expiry.ts`), where it is awaited inside a 30s invocation, logs its
result on **every** path, and is gated three ways:

1. Malaysian **Sunday, after 2pm** — the café trades Sundays only, and this also
   stops an accidental mid-morning close from burning the day's single send on a
   partial service. The Wednesday midweek stock rule invokes the same handler, and
   the `day !== 0` gate is what stops it emailing an empty RM0 report.
2. **`cafeStatus === 'CLOSED'`** — the summary describes a finished service, so it
   waits for the cashier's Close Café rather than guessing from the clock.
3. A **`DAILY_SUMMARY#{date}`** marker (settings table, `SK=META`) written *only*
   after a confirmed send. That makes it exactly-once, and because a failure leaves
   no marker, every later run in the window retries it for free.

The EventBridge window widened from `cron(0/30 1-7 ? * SUN *)` to `1-9` (9am–5pm
MYT) so a close after 3:30pm still has a run left to carry the summary — otherwise
the fix would have reintroduced the same "no second chance" shape it removes. This
is a **CDK change, so the backend must be deployed** for it to take effect.

`closeCafe` now carries a comment block at the old call site saying explicitly that
no email belongs there, because the tempting fix is to put it back.

## The date bug, and how it survived a test

The email subject rendered a day early — "Saturday, 1 August 2026" for the 2 August
service — because `formatDate` in `backend/src/lib/email.ts` parsed midnight MYT
and then called `toLocaleDateString` with **no `timeZone` option**, so it formatted
in the runtime's zone. On Lambda that is UTC, which lands at 16:00 the previous day.
Now pinned to `Asia/Kuala_Lumpur`.

**`npm test` is now `TZ=UTC jest`.** The existing date assertion passed against the
broken code, because the dev machine's zone is already `Asia/Kuala_Lumpur` — the
ambient zone made the wrong output look right, so the test could not see the bug it
was meant to guard. Lambda is UTC; the suite now matches production.

Two traps worth recording:

- **Setting `process.env.TZ` in `setupFiles` does NOT work.** Node resolves and
  caches the process timezone before `setupFiles` runs, so the variable changes but
  `Intl.DateTimeFormat().resolvedOptions().timeZone` does not. It has to come from
  the environment *before the process starts*, which is why the pin lives in the
  `package.json` script and not in the jest config.
- **A bare `npx jest` bypasses the pin entirely.** `backend/tests/setup.ts` now
  prints a warning when the resolved zone is not UTC, and the `test-suites` skill
  was corrected — it had been recommending `npx jest --testPathIgnorePatterns
  integration`, which would have quietly undone this.

`backend/src/lib/date.ts` is new and is the single source of truth for the UTC+8
conversion (`malaysiaToday` / `malaysiaClock` / `malaysiaDayStartUtc`), extracted
from `routes/staffcode.ts`, which re-exports it so existing imports keep working.

## Dashboard redesign

Restructured from eight equal-weight sections into **two zones**:

- A compact live **"Right now"** strip — pending, preparing, oldest wait — rendered
  **only** when the selected date is today, because those numbers are meaningless
  for a past Sunday.
- An **analysis zone led by revenue**, then the charts and the supporting detail.

Three visualisations, plain HTML/CSS with **no charting library**: an order-source
stacked bar, top items as horizontal bars, and session comparison as **two small
charts with orders and revenue on separate scales** — deliberately not a dual-axis
chart, which invites false correlation between two unrelated units.

The **order-source breakdown** is the feature the café asked for: how many orders
were walk-ins at the counter, ministry pre-orders, or placed by customers on their
phones. Derived client-side from `isWalkUp` / `isPreOrder` with explicit
precedence (pre-order wins, since a pre-order raised at the counter would set
both), over the same `PENDING`/`PREPARING`/`READY`/`ARCHIVED` statuses as the
headline order count — so the three segments sum to it exactly, and cancelled and
expired orders are excluded from both. **No backend change**; the fields were
already on the daily report.

Also: zero-order dates collapse to a single line instead of four empty panels;
`Latest Stock Count` became a collapsed `<details>`; headings name the date being
viewed, so a past Sunday no longer says "Today's Discounts". Sunday page height
dropped **4006px → 3087px**. Two unescaped interpolations in `featuredAuditHtml`
were fixed and a duplicate `fmtTime` removed.

## No surprise email on deploy

Worth stating plainly, because the change makes a cron send email: **nothing will
send when the backend is deployed.** The Sunday-and-after-2pm-MYT gates mean the
first possible send is **Sunday 23 August, from 2pm MYT**, and only once the
cashier has closed the café. Deploying midweek, or on a Sunday morning, sends
nothing — the cron logs its skip reason and returns.

## Changes

### Backend
- `backend/src/expiry.ts` — new `sendDailySummary()`, awaited last in the handler
  (after expiry and archiving, which must not queue behind an SMTP call); three
  gates; logs every path. `checkLowStock` switched to the shared MYT helpers, and
  its date fixed from `toISOString()` to `malaysiaToday()`.
- `backend/src/lib/daily-summary.ts` — **new.** Builds and sends the end-of-day
  summary; returns a boolean the caller checks.
- `backend/src/lib/date.ts` — **new.** Single source of truth for MYT conversion.
- `backend/src/lib/email.ts` — `formatDate` pinned to `Asia/Kuala_Lumpur`.
- `backend/src/routes/pos.ts` — the un-awaited send removed from `closeCafe`, with
  a comment block saying it must not come back; `summarizeDailyRevenue` and
  `sendDailySummaryEmail` moved out to `lib/daily-summary.ts`; `malaysiaToday`
  imported from `lib/date`.
- `backend/src/routes/staffcode.ts` — `malaysiaToday` implementation moved to
  `lib/date.ts`, re-exported here.
- `backend/package.json` — `test` and `test:watch` now pin `TZ=UTC`.
- `backend/tests/setup.ts` — warns when the resolved zone is not UTC, with the
  reasoning.
- `backend/tests/daily-summary-cron.test.ts`, `backend/tests/email-date.test.ts` —
  **new.** The gates, the exactly-once marker, retry-on-failure, that `closeCafe`
  no longer sends, and the MYT date rendering.
- `backend/tests/daily-summary.test.ts` — import path follows the move.

### Infra
- `infra/lib/infra-stack.ts` — expiry cron `cron(0/30 1-7 ? * SUN *)` → `1-9`.

### Frontend
- `frontend/js/admin-dashboard.js`, `frontend/css/admin.css` — the redesign above.
  No new files, so no `sw.js` `SHELL` change.

### Documentation and skills updated in this pass
- `.kiro/skills/db-schemas/SKILL.md` — record types 15 (`LOW_STOCK_ALERT#{date}`)
  and 16 (`DAILY_SUMMARY#{date}`), and the write-only-on-success rule;
  `malaysiaToday`'s new home.
- `.kiro/skills/order-lifecycle/SKILL.md` — corrected the cron description (it said
  "every 5 minutes" and listed three jobs; it is every 30 min on Sundays and does
  five), and a new section: closing the café does **not** send the email.
- `.kiro/skills/invariants/SKILL.md` — `lib/date.ts` added to the single-sources
  table (noting the one remaining copy in `preorder.ts`); two new backend-shape
  invariants (no un-awaited work outliving a handler; date-keyed markers written
  only on success); one new test-teeth invariant (a test that depends on the
  machine's timezone is not a test). Frontmatter `description` extended so it is
  found for emails, scheduled work and timezones.
- `.kiro/skills/test-suites/SKILL.md` — **it was actively wrong after this change**:
  it recommended a bare `npx jest`, which drops the UTC pin. Now says `npm test`,
  with the two traps. Also: the category-2 row claiming the café close "sends an
  end-of-day email" is no longer true, and a new subsection records that the risk
  **moved rather than disappeared** — a live run on a Sunday afternoon now leaves
  the cron to email a summary containing the test order *and* burns that date's
  single send, suppressing the genuine one. New suites added to the table.
- `.kiro/skills/api-reference/SKILL.md` — `PUT /api/pos/cafe/close` documents its
  side effects and that it sends no email.
- `.kiro/steering/project.md` — `date` and `daily-summary` added to the `lib/`
  layout; the "EventBridge cron, 5min" description corrected.
- `.kiro/steering/conventions.md` — MYT dates added to the single-sources list.
- `docs/architecture.md` — §6.4 cron schedule corrected, §6.5 end-of-day flow split
  into request path vs cron with the root cause, §7.1 EventBridge expression and
  expiry-Lambda responsibilities updated.
- `docs/feature-history.md` — a **2026-08-17 Sprint — v1.72.0** entry; the "Email
  notifications" TODO marked done, since both alerts now ship.

`.claude/skills/invariants/` and `.claude/skills/test-suites/` stubs had their
`description` frontmatter re-synced to match the `.kiro` originals (and the
test-suites stub's bare-`jest` short version corrected). `.kiro` remains
authoritative.

## Verification (this pass)

Re-run against the tree as committed, not taken from the previous pass:

- `cd backend && npx tsc --noEmit` → exit 0.
- `cd backend && npm test` → **17 suites, 304 passed, 18 skipped, 322 total.** The
  18 skips are `integration.test.ts` self-skipping without credentials and without
  `RUN_LIVE_WRITE_TESTS=1`. That is the correct, expected behaviour — a skip is not
  a pass, and it is reported as a skip.
- `cd infra && npx tsc --noEmit` → exit 0.
- `npm run version:check` → all six markers at v1.72.0, SHELL complete.

**Not run, deliberately:** `backend/tests/integration.test.ts` with credentials or
`RUN_LIVE_WRITE_TESTS=1`, and every Playwright journey. They write to the live
production café. Nothing in this release was verified against production.

**Therefore unverified by execution:** the cron's real behaviour on a live Sunday
(the gates and the marker are covered by unit tests with mocked DynamoDB only), the
actual delivery of the email, and the dashboard rendered in a real browser against
real data.

## Follow-ups NOT done in this release

Continuing the lettering above; a–f still stand.

g. **`backend/src/routes/preorder.ts:108` still does its own MYT arithmetic** —
   `now.getTime() + 8 * 60 * 60 * 1000` — and should use `lib/date.ts`. Verified as
   the only remaining copy outside `lib/date.ts` itself. **Deliberately not folded
   in:** it sits on the v1.71 pre-order path, which is freshly written and not yet
   deployed, and changing it in the same release would make any pre-order date
   regression impossible to attribute between the two changes.

h. **The 1-hour PENDING sweep in `expiry.ts` (~lines 30–36) is the only loop in
   that file with no `ConditionExpression`.** Verified: its `UpdateCommand` writes
   `status = EXPIRED` and `REMOVE expiresAt` keyed only on `PK`/`SK`, with nothing
   asserting the row is still PENDING. Safe today because the query itself selects
   on PENDING via the GSI, but that read and this write are not atomic — an order
   approved in the gap between them would be expired out from under the cashier.
   It is the weakest guard in the file.

i. **Dashboard: two things could not be verified.** iPad Safari's `<details>`
   behaviour for the collapsed `Latest Stock Count` is untested on the actual
   counter device. And the live "Right now" strip was only ever seen with pending
   and preparing at **0**, so the oldest-wait-minutes branch never rendered — that
   code path is unexercised, not merely unstyled.

j. ~~**`computePastSundays` in `admin-dashboard.js` derives dates from UTC** —
   `d.toISOString().slice(0, 10)` (line 27) combined with a local `getDay()` (line
   25). Near the MYT midnight boundary the list can drift by a day, offering a
   "Sunday" that is really Saturday. The same UTC-vs-MYT confusion as the email bug,
   on the frontend; `dashboardSelectedDate` (line 18) shares it.~~

   **CLOSED in v1.75.0** — see the v1.75.0 section at the end of this file. It was
   **wider than this entry recorded: eight sites**, not two, and
   `admin-preorder.js` could stamp `serviceDate` as Saturday. One `mytToday()`
   helper now lives in `admin.js:109` (the frontend counterpart of
   `backend/src/lib/date.ts`) and is used at every one of them, `computePastSundays`
   and `dashboardSelectedDate` included.

## Still for the user to run — v1.72.0

Nothing is deployed and nothing is pushed. **Backend must land first**, because the
frontend of v1.71.0 depends on routes that do not exist in production yet:

```
npm run deploy:backend      # CDK → ap-southeast-5, account 956288449190
npm run deploy:frontend     # git push; Actions publishes Pages
```

The backend deploy is required for the email fix **and** for the widened cron
window (a CDK change — a frontend-only deploy leaves the email broken). This
release touches `frontend/**`, so the Pages workflow will fire on the push.

Both v1.71.0 and v1.72.0 are unpushed, so a single `deploy:frontend` publishes
both.

---

# Third release pass — v1.73.0

A third release on the same day, on top of **v1.72.0, which is already pushed and
deployed** (backend via CDK, frontend via Pages — the live site serves
`rlc-cafe-v1.72.0`). So this is a clean follow-on release, not another stacked
unpushed one. One piece of work: **web push was silently dead in production.**

## Version

**v1.72.0 → v1.73.0** (minor — it restores a dead user-facing feature and changes
how the backend is configured at runtime), via a single
`npm run version:bump -- minor`. All six markers and the top `changelog.json`
entry were written together; none was hand-edited. `npm run version:check`
reports all six agreeing at v1.73.0 with SHELL complete.

No new `frontend/js` or `frontend/css` file — this release does not touch
`frontend/` at all except the four version markers and `changelog.json` — so the
`sw.js` `SHELL` array is unchanged.

`frontend/changelog.json` v1.73.0 carries **one** volunteer-facing line: that a
customer who taps "Yes, notify me" on the tracking page and allows notifications
is now actually alerted when their order is confirmed and when it is ready.
Deliberately says nothing about SSM or VAPID — a volunteer cannot act on either.

## The root cause

`backend/src/lib/push.ts` read the VAPID keys from **Lambda environment
variables only**:

```ts
const VAPID_PUBLIC = process.env.VAPID_PUBLIC_KEY || '';
const VAPID_PRIVATE = process.env.VAPID_PRIVATE_KEY || '';
if (VAPID_PUBLIC && VAPID_PRIVATE) { webpush.setVapidDetails(...); }
```

and `infra/lib/infra-stack.ts` supplied them as
`VAPID_PUBLIC_KEY: process.env.VAPID_PUBLIC_KEY || ''`. **Any `cdk deploy` from a
shell that had not exported the keys therefore overwrote the live values with
empty strings.** There is no way to tell from the stack template that this
happened; a successful deploy is indistinguishable from one that just destroyed
the feature.

`sendOrderPush` then hit, literally:

```ts
if (!VAPID_PUBLIC || !VAPID_PRIVATE) return; // VAPID not configured, skip silently
```

No log line, ever. `GET /api/push/vapid-public-key` returned
`500 {"error":"VAPID not configured"}` in production the whole time.

## Customer impact, stated plainly

This is the part worth remembering. `frontend/js/track.js` offers a notifications
banner ("Yes, notify me"). On tap it calls `Notification.requestPermission()`
**first**, the customer **granted browser permission**, and only then did it fetch
the public key — which came back from a 500 as `undefined`, so
`pushManager.subscribe()` threw.

So the customer spent **the one notification permission they will ever grant** —
browsers do not re-prompt after a decision, and a person who has been burned once
does not go into site settings to try again — and received nothing. Not a
degraded feature: a feature that took something from the customer and gave
nothing back. That is why the "skip silently" line is now recorded as a defect in
the `invariants` skill rather than as defensive coding.

## The fix

- VAPID config moved to **SSM under `/rlc-cafe/`**: `VAPID_PUBLIC_KEY` (String —
  it is served to every browser, so it is not a secret), `VAPID_PRIVATE_KEY`
  (SecureString), `VAPID_SUBJECT` (String). `push.ts` reads it through an
  awaited, cached `ensureVapidConfigured()`; `setVapidDetails` is re-applied only
  when the public key actually changes, so a warm Lambda does no extra work per
  push. **Nothing in the deploy path can wipe an SSM parameter.**
- **The silent `return` is gone.** A missing or malformed config now
  `console.error`s, naming **which half** is missing, where it looked, and the
  `aws ssm put-parameter` command that fixes it. A config web-push actively
  *rejects* is logged separately with the subject and the key lengths — web-push
  throws at `setVapidDetails`, not at send time, so that is the only place the
  detail exists.
- `backend/src/routes/push.ts` no longer reads `process.env.VAPID_PUBLIC_KEY`. It
  goes through `ensureVapidConfigured()`, which returns a key only once web-push
  has accepted the **whole triple**. Serving a public key whose private half is
  absent would let the browser subscribe *successfully* and be undeliverable
  forever — worse than an honest 500, because it burns the permission prompt
  while looking like it worked.
- `VAPID_SUBJECT` changed from `mailto:admin@rlccafe.com` — **a domain this
  project does not own** — to `https://153.oasisofcare.org`. Push services treat
  the `sub` claim as the operator contact and may reject a bogus one.
- All three `VAPID_*` env vars **removed from the Lambda** in `infra-stack.ts`,
  with a comment saying they must not come back: the point is that there is
  nothing left for a fresh deploy shell to wipe.
- `ORIGIN_VERIFY_SECRET` no longer defaults to `CHANGE_ME_WHEN_CLOUDFRONT_ENABLED`.
  That literal is committed in a **public** repository, so the day anyone flipped
  `ENFORCE_ORIGIN_HEADER` to `'true'` the "shared secret" would have been public
  knowledge and the front-door check bypassable with one header. It is now
  `requireSecret`d when `ENFORCE_ORIGIN_HEADER === 'true'`, not emitted at all
  otherwise, and the placeholder is on the `requireSecret` denylist.

## The near-miss: a second latent copy of the same bug

Found while moving the config, and worth more than the fix that prompted it.
`getEmailConfig` called `GetParametersByPath` with **no pagination**:

```ts
const result = await ssm.send(new GetParametersByPathCommand({
  Path: SSM_PREFIX, WithDecryption: true,
}));   // no NextToken loop
```

`GetParametersByPath` returns at most **10** parameters per call. `/rlc-cafe/`
already holds **7**. Three more parameters — and this release adds three — and
`GMAIL_APP_PASSWORD` would have fallen off the first page, `getEmailConfig` would
have quietly fallen back to `process.env`, found nothing, and the end-of-day
email would have failed **in the identical indistinguishable way**: a missing
value looking exactly like a feature nobody turned on. The v1.72.0 email fix
would have been undone by arithmetic.

Both configs now share **one** paginated, 5-minute-cached fetch of the whole
prefix (`loadSsmParams`), so there is one place to get this right and one cache
to warm. The invariant is recorded as: *a paged AWS list call is paginated even
when today's count is under the page size.*

## `/rlc-cafe/jwt-secret` already exists and nothing reads it

Worth recording so the next person does not conclude the migration was done.
The parameter `/rlc-cafe/jwt-secret` is present in SSM but **no code reads it** —
`JWT_SECRET` still comes from the deploy environment. Someone began this
migration and stopped. It was **not** finished here: see follow-up (k).

## Changes

### Backend
- `backend/src/lib/ssm-config.ts` — new `loadSsmParams()`: paginates on
  `NextToken`, caches the whole `/rlc-cafe/` prefix for 5 minutes, never throws
  (returns `{}` so callers fall back to `process.env` for local dev). New
  `getVapidConfig()`; `getEmailConfig()` rewritten to read the shared cache. New
  `resetSsmConfigCache()` test seam. `DEFAULT_VAPID_SUBJECT` =
  `https://153.oasisofcare.org`.
- `backend/src/lib/push.ts` — new exported `ensureVapidConfigured()` (async,
  caches which public key is applied to the web-push singleton, logs loudly on
  every failure path) and `resetVapidState()`. `sendOrderPush` awaits it and logs
  the order and title it is *not* sending. The module-level constants and the
  `// skip silently` return are gone.
- `backend/src/routes/push.ts` — `GET /api/push/vapid-public-key` resolves through
  `ensureVapidConfigured()` instead of `process.env`, with a comment explaining
  why a partial config must not be served.
- `backend/tests/push-vapid.test.ts` — **new, 16 tests**: SSM sourcing, the
  subject default, caching (three pushes, one SSM call), the shared cache with
  the email config, **pagination** (keys on the second page are still found), the
  env fallback, that a missing config logs and names only the missing half, that
  a malformed config is logged and sends nothing, delivery to every stored
  subscription, 410 cleanup, and the route including its refusal to serve a
  public key with no private counterpart. SSM, DynamoDB and `web-push` are all
  mocked — nothing is sent, nothing is read for real.

### Infra
- `infra/lib/infra-stack.ts` — all three `VAPID_*` env vars removed from the
  Lambda environment (with a do-not-reintroduce comment);
  `ORIGIN_VERIFY_SECRET`/`ENFORCE_ORIGIN_HEADER` conditionally emitted and
  `requireSecret`d; `CHANGE_ME_WHEN_CLOUDFRONT_ENABLED` added to the
  `requireSecret` placeholder denylist; the SSM policy comment now states what
  the prefix holds and why `WithDecryption` needs no extra `kms:Decrypt` grant
  (AWS-managed `alias/aws/ssm`). A `FOLLOW-UP` comment marks `JWT_SECRET` as the
  remaining env-var secret — **a comment, not a code change.**
  **This is a CDK change, so the backend must be deployed** for the env vars to
  actually leave the function.

### Frontend
- Version markers only (`sw.js`, `index.html`, `pos.html`, `admin.html`,
  `reports.html`) plus `changelog.json`. No behaviour change — see follow-up (l),
  which is the frontend half of this bug and is **not fixed in this release**.

### Documentation and skills updated in this pass
Written by the implementing agent and **verified line by line during this pass**,
not taken on trust:

- `.kiro/skills/invariants/SKILL.md` — four new assertions: a feature disabled by
  missing config must log loudly every time it skips; runtime config that a
  deploy can silently empty does not belong in the Lambda environment; a partial
  config is treated as no config; a paged AWS list call is paginated even under
  the page size. Frontmatter `description` extended so the skill is found for
  config and SSM questions.
- `.kiro/skills/test-suites/SKILL.md` — `push-vapid.test.ts` added to the
  offline-suites table, noting SSM/DynamoDB/web-push are all mocked.
- `.kiro/steering/project.md` — new **Runtime configuration** section with the
  full `/rlc-cafe/` parameter table and why these are not env vars.
- `docs/deployment.md` — new **Runtime configuration (SSM Parameter Store)**
  section: the parameter table, why they are not env vars, a key-rotation recipe
  that prints only the public key, the `curl` verification, and a
  deploy-environment-secrets table. **The `/rlc-cafe/` parameters had been
  documented nowhere at all.**

Added in this pass, because the table in the release brief pointed at rows the
implementing agent had not covered:

- `.kiro/skills/api-reference/SKILL.md` — the `GET /api/push/vapid-public-key`
  row. **Judgement call:** no endpoint was added, removed or re-authed, so
  strictly the "method, path, or auth requirement" trigger did not fire. Edited
  anyway, because the row said "Get VAPID public key" of an endpoint that
  returned 500 in production for weeks, and the useful fact for a future
  debugger is that it now answers only once web-push has accepted the full
  triple and that a partial config is still a 500.
- `.kiro/skills/invariants/SKILL.md` — `ssm-config.ts` added to the
  **single-sources-of-truth table** (the implementing agent added the prose
  invariant at the bottom but left the table stale).
- `.kiro/steering/conventions.md` — same module added to its single-sources list.
- `.kiro/steering/project.md` — `ssm-config` added to the `lib/` **layout block**,
  which had never listed it.
- `docs/architecture.md` §8 Security — it listed JWT-in-env and PIN hashes as the
  places secrets live, which after this change is actively misleading. Now names
  the SSM prefix and why VAPID must not go back into the environment.
- `docs/feature-history.md` — a **2026-08-17 Sprint — v1.73.0** entry, and the
  Post-June "Push notifications" line annotated: it was built then, but was
  **dead in production until v1.73.0**.

**Not updated, deliberately:** `.kiro/skills/db-schemas` (no table, GSI, TTL or
record attribute changed — SSM parameters are not DynamoDB, and the `PUSH_SUB#`
record type and its TTL are untouched), `.kiro/skills/order-lifecycle` (no status
transition, guard or food-counter effect changed; push is sent on approve and
ready exactly as before), `.kiro/skills/pricing-rules` (no money touched),
`.kiro/skills/release-flow` and `.kiro/steering/release-checklist.md` (the
release and deploy procedure itself is unchanged), and `docs/requirements.md`
(nothing in it is contradicted). No new skill was created — runtime config is
covered by `project.md` plus the deployment runbook and is not a subsystem of its
own, so there is no new `.claude/skills/` stub either. No `.claude/skills/` stub
was touched at all: the only `description` frontmatter changed is `invariants`,
whose stub the v1.72.0 pass already re-synced to the same wording.

## Verification (this pass)

Re-run against the tree as committed, not carried over from the brief:

- `cd backend && npx tsc --noEmit` → exit 0.
- `cd infra && npx tsc --noEmit` → exit 0.
- `cd backend && npm test` → **18 suites, 320 passed, 18 skipped, 338 total.**
  The 18 skips are `integration.test.ts` self-skipping without credentials and
  without `RUN_LIVE_WRITE_TESTS=1`. That is correct behaviour, and it is reported
  as a **skip, not a pass**.
- `npm run version:check` → all six markers at v1.73.0, SHELL complete.
- `git check-ignore -v .impeccable` → ignored via `.gitignore:39`; confirmed
  absent from the commit.
- Diff read end to end for credentials: no key, PIN or token in it. The
  `docs/deployment.md` rotation recipe prints the **public** key only.
- The two `frontend/js/track.js` line references in follow-up (l) were read in
  the file rather than trusted: `Notification.requestPermission()` at **:115**,
  the unchecked `fetch` at **:119-120**, `urlBase64ToUint8Array(publicKey)` at
  **:124**, the second bare `requestPermission()` at **:954**.

**Not run, deliberately:** `backend/tests/integration.test.ts` with credentials
or `RUN_LIVE_WRITE_TESTS=1`, and every Playwright journey. They write to the live
production café. The hazard is now sharper than it was: an Order Flow run on a
**Sunday afternoon** writes `DAILY_SUMMARY#{date}`, which then **suppresses the
genuine service summary email** for that date.

**Therefore unverified by execution:**

- That `/rlc-cafe/VAPID_PUBLIC_KEY` and `/rlc-cafe/VAPID_PRIVATE_KEY` **exist and
  hold valid keys**. Nothing in this pass read SSM. If they were never created —
  or if the empty env vars were the *only* place the keys ever lived — then after
  deploying, `GET /api/push/vapid-public-key` will still 500, only now with a
  `[PUSH]` CloudWatch line naming exactly which parameter is missing. **Check
  that endpoint after the backend deploy** (the `curl` is in `docs/deployment.md`).
- That `/rlc-cafe/jwt-secret` exists. That claim comes from the implementing
  agent and is recorded as such; it is not load-bearing for anything shipped,
  since no code reads it.
- Real end-to-end delivery of a notification to a real device.

## Follow-ups NOT done in this release

Continuing the lettering above; a–j still stand.

k. **`JWT_SECRET` is the last runtime secret in the Lambda environment**, and
   should move to SSM — `/rlc-cafe/jwt-secret` already exists there and is read
   by nothing. **Deliberately deferred:** it is on the auth hot path, every
   protected request verifies against it, and a wrong move logs out every
   volunteer mid-service. It is also not exposed to the bug this release fixes,
   because `requireSecret()` makes synth *fail* rather than defaulting it to `''`.
   The implementing agent left a `FOLLOW-UP` comment at the declaration in
   `infra/lib/infra-stack.ts` and **changed no code**.

l. **The frontend half of this bug is still there — `frontend/js/track.js`.**
   Two separate faults, both `frontend-pwa`'s:

   - **`:115-124`** — `await Notification.requestPermission()` runs **before**
     the key is fetched, and `vapidRes.ok` is never checked. So a backend failure
     still burns the permission prompt and then throws inside
     `urlBase64ToUint8Array(undefined)`, landing in a `catch` that only
     `console.error`s: the customer sees nothing at all. The correct order is
     fetch-and-validate the key first, ask for permission second, and tell the
     customer when it could not be set up. **This release makes the backend stop
     failing; it does not make the frontend survive a backend failure.**
   - **`:954`** — `if ('Notification' in window && Notification.permission ===
     'default') Notification.requestPermission();` fires on page load with **no
     `subscribe()` anywhere after it**. Verified in the file: this sits on the
     branch where an `orderId` *is* present (the brief described it as the
     non-order branch — it is the other one), so anyone opening a tracking link
     directly, without the `push_offer_order` sessionStorage handoff from the
     order flow, is prompted for permission that is then never used for anything.
     A permission request with no subscription behind it should simply be
     deleted.

m. **VAPID key rotation is not instant.** A rotated parameter is picked up after
   the 5-minute config cache expires in each warm sandbox, plus however long
   until cold starts replace the rest. Expect **up to ~5 minutes** of mixed
   behaviour, during which some invocations sign with the old key. Not a problem
   for the café's traffic, but do not rotate and then immediately conclude from
   one `curl` that it failed.

## Still for the user to run — v1.73.0

Nothing is pushed and nothing is deployed. **The backend deploy is the one that
matters**, and it must happen for this release to do anything at all:

```
npm run deploy:backend      # CDK → ap-southeast-5, account 956288449190
npm run deploy:frontend     # git push; Actions publishes Pages
```

- `deploy:backend` is **required**: the fix is entirely backend + CDK. Without it
  the Lambda keeps its empty `VAPID_*` env vars and push stays dead.
- `deploy:frontend` ships only the version markers and the changelog entry, so
  the "What's new?" modal and the footers stop saying v1.72.0. This commit **does
  touch `frontend/**`**, so the Pages workflow will fire on the push. (Had it
  been backend-only, the frontend would silently never have shipped.)
- **After the backend deploy, verify the endpoint** — this is the one check that
  distinguishes "fixed" from "now failing loudly":

  ```
  curl -s https://hcydppml1a.execute-api.ap-southeast-5.amazonaws.com/prod/api/push/vapid-public-key
  ```

  A `200 {"publicKey":"..."}` means push is live. A `500` means the SSM
  parameters are missing or malformed — `/aws/lambda/rlc-cafe-api` will now carry
  a `[PUSH]` line saying which half, which is the whole point of the change.

---

# Per-item notes and editable collection times

> **Dated 2026-08-17 by filename, but this work was done on 2026-08-18.** It is
> appended here rather than split into `docs/update-20260818.md` at the user's
> request, because it continues the T5 / follow-up-(b) thread above. Read the date
> in this heading, not the one in the filename.

## Context

Two items, plus one security finding made while implementing them.

1. **T5 of `docs/graph-spec-20260817.md`** — Mei Yii's report, verbatim:
   *"Multiple drink cannot add request to each item."* `notes` was a single
   per-**order** string, so a customer ordering three drinks had no way to say
   "less sugar" about one of them. They had to write it into the order note and
   hope the barista could work out which cup it meant.

2. **Follow-up (b) of this same file** — *"When they re-order, should allows them
   to change the option if they need to."* Follow-up (b) recorded that a
   pre-order's collection time lives only in the `notes` prefix and so "cannot be
   amended without delete-and-re-order". That is now amendable, and (b) is marked
   addressed above.

3. **A live XSS on the barista and cashier screens**, found while adding the
   per-item note render sites. Not planned work; see below.

## 1 — Per-item special requests

### Backend

`backend/src/routes/orders.ts`:

- `export const ITEM_NOTE_MAX_LENGTH = 80` and
  `export function validateItemNote(raw)`, shared by `createOrder` and
  `modifyOrder`. One helper on purpose: a restriction enforced only on create
  makes the edit endpoint a bypass of it, which is the create/edit-parity class of
  bug found in v1.71 and now recorded in the `invariants` skill.
- Module-local `withItemNote()` attaches `note` to a stored item **only when
  non-empty**. So a record for an order with no item notes is **byte-identical**
  to what shipped before: no migration, no backfill, no `note: ''` sprayed across
  historical-looking items.
- Both error paths are 400 and both are raised **before any DynamoDB write**, so a
  rejected note cannot leave `foodReserved` moved:
  `'Item note must be a string'` and `'Item note cannot exceed 80 characters'`.

**Why 80 and not the per-order 200.** An item note renders one-per-line on the
barista prep card and on the POS queue card, where vertical space is the binding
constraint — a per-order note renders once, an item note renders once per line. A
four-drink order at 80 each keeps the total free text in the same order of
magnitude as a single order note. The cap is measured on the **trimmed** value, so
trailing whitespace out of a text input cannot push an otherwise-legal note over
it.

**Nothing was added to `backend/src/lib/pricing.ts`**, deliberately: a note is not
a price and affects no money. It never reaches `priceLine`,
`summarizeOrderDiscount`, `totalAmount`, `grossAmount` or `discountOffset`.

The per-order `notes` string is **kept unchanged** and still carries order-wide
requests. Only its frontend label and placeholder were repurposed, so the two
roles are unambiguous to the customer and to the cashier ("Order note:" on the
POS).

### Frontend

- **`frontend/js/app.js`** — the customer cart **no longer merges lines** by
  `id + variant`. Every drink is now its own independently-notable line at
  `qty: 1`. A one-time expansion splits legacy `localStorage` carts with
  `qty > 1` into N lines. The per-line `[−]` / `[+]` controls and the qty count
  were **removed as redundant** once every line is a single drink; the ✕ remove
  is kept. Each row gets an always-visible `maxlength="80"` note input. New
  module-level `escHtml`.

  **The user's rationale for de-merging**, recorded because it is a product
  decision and not an implementation detail: orders are mostly a single drink and
  rarely exceed four, so the cost of losing quantity controls is negligible next
  to the benefit of every drink being individually notable. A merged line cannot
  carry two different notes.

- **`frontend/js/track.js`** — the read view shows each item's note; the edit flow
  shows and edits them. **Quantity controls are deliberately kept here.** A
  *stored* order's items can legitimately have `quantity > 1` — walk-up orders and
  every historical order — and stored items are **not** de-merged. Only the
  customer cart stopped merging; the backend still handles `quantity > 1`
  everywhere.

- **`frontend/js/pos.js`** — per-item notes on the queue card, in the order detail
  and in the in-POS prep view, styled distinctly from the order-level note.
- **`frontend/prep.html`** — per-item notes for the barista.
- **`frontend/css/style.css`** — supporting styles only.

- **`frontend/js/pos-walkup.js` — unchanged, deliberately.** It renders a cart it
  builds itself and never renders a stored order's `items`, so there is no
  per-item-note render site in it. Its merge behaviour was left alone on the
  user's instruction.

## 2 — Editable pre-order collection time

No `collectionTime` attribute was added. The user decided against one, and there
still isn't one — the time continues to live only inside the
`[PRE-ORDER: CODE] Collect: …` notes prefix.

`backend/src/routes/orders.ts`:

- `export function resolveCollectionTime(preorderRecord, raw)`, shared by create
  and edit. The allowed list is the link's own `collectionOptions`, falling back
  to `DEFAULT_COLLECTION_OPTIONS` **imported from `routes/preorder.ts`**, not
  re-declared. Off-list → `400 {error:'Invalid collection time'}`; non-string →
  `400 {error:'collectionTime must be a string'}`. Matching is exact after
  trimming both sides — no case folding, no fuzzy match, because the value is
  echoed verbatim into the prefix the cashier collects against.
- A **hard-deleted link falls back to the defaults** — fail-closed. The customer
  can still change their slot, but never to free text of their own.
- **`createOrder` now validates it too.** It had accepted an arbitrary string, so
  restricting only the edit path would have been pointless: a crafted *create*
  would simply set the arbitrary value up front. This is the parity rule biting in
  the less obvious direction, and it is now written down in the `invariants`
  skill. No legitimate client is affected — the customer page only ever submits a
  value from the same list.
- **`modifyOrder`** rebuilds the prefix through the existing
  `preorderNotesPrefix` / `composePreorderNotes` / `splitPreorderNotes` helpers,
  so the format string still exists in exactly one place. The **code always comes
  from the stored order record, never the request body**, so a client can neither
  forge a code nor set an arbitrary time.
  - The `notes = :n` clause is now emitted when `body.notes !== undefined`
    **or** a validated `collectionTime` applies. This was a real defect in the
    making: a client may change nothing but the time, and the time lives *inside*
    `notes`, so under the old condition that request would have written nothing at
    all.
  - When `body.notes` is absent the stored customer portion is preserved verbatim
    rather than blanked while the prefix is rewritten around it.
  - A validated time **creates** the prefix on a pre-order that had none — one
    placed without a collection time, or one predating the convention.
  - `expiresAt` is still not touched. A pre-order's remains the ISO
    `serviceEndTime`, which is the only input `expirePreOrders()` has.
  - The MODIFY audit line gained `collectionTime` when one was applied.
- The pre-existing pre-order edit restrictions (`preorderItemRejection()` —
  drinks-only, `eligibleItems`, `excludedOptions`) are **unchanged and still
  enforced**; regression-tested.

`getOrder` now returns, **only when `isPreOrder === true`**, `collectionTime`
(parsed from the stored prefix by the new
`export function parsePreorderCollectionTime`) and `collectionOptions`
(`string[]`).

**Why from `getOrder` and not `GET /api/preorder/validate`:** that endpoint
enforces the link's own ordering window, so it would refuse to describe a link
whose window has closed — and the edit window is the **order's** PENDING status,
not the link's. Asking it would lose the picker on a legitimate edit. The extra
DynamoDB read is guarded behind the `isPreOrder` flag because this handler is
polled every 7s by `track.html`; an ordinary order must not pay for it.

`frontend/js/track.js` gains a collection-time radio group in the pre-order edit
form, reusing `app.js`'s existing `.collection-section` / `.collection-radios` /
`.collection-radio` classes and `input[name="collectionTime"]`, preselected from
`order.collectionTime`. It renders **nothing** when `collectionOptions` is absent
or empty — so the page does not break against today's still-undeployed production
API, which returns neither field.

## 3 — Escaping: a live XSS in the cashier's authenticated session

Found while adding the note render sites. **Every site below was verified against
the committed code before anything was changed**, and one claim that reached this
pass was a false positive.

- **`frontend/prep.html` escaped nothing at all.** `${i.name}`, `${i.variant}`,
  `${o.customerName}` and `${o.notes}` were raw interpolations into `innerHTML`.
  The order-level `notes` is **customer-typed free text**, so this was a live
  injection path onto the barista's screen. An inline `esc()` covering all five of
  `& < > " '` now wraps every order/item/customer interpolation on the page,
  including the pre-existing ones. Inline is the right call there: the page has a
  self-contained script and loads only `js/config.js`.

- **`frontend/js/pos.js`** had raw `customerName`, `items[].name`,
  `items[].variant` and `notes` on the queue card, in the order detail and in the
  in-POS prep view. The POS session holds a **CASHIER JWT in `sessionStorage`**,
  so script injected there runs with till privileges — this was
  privilege-bearing, not cosmetic. All of them now go through the
  **already-existing** `escapeHtmlPos`, which escapes all five characters and so
  is safe in a quoted attribute as well as in text.

- **The shape of the bug is the lesson.** The same `customerName` field was
  *already escaped* in the v1.71 dialogs (`askStaffPrice`,
  `confirmReleaseNotToday`) and unescaped on the card behind them. The newer code
  got it right; the v1.0 hot path was never revisited. Escaping is a property of
  the **render site**, not of the field — recorded as a new invariant.

- **False positive, deliberately not changed.** `pos.js` builds a cancellation
  string from a raw `customerName` and passes it to `showCancelToast`, which
  assigns **`textContent`**. That is a safe sink; escaping first would show the
  cashier a literal `&quot;`. Same for `showNameFlash`. Both verified in the file
  and left alone. The counterpart rule — *a `textContent` sink must not be
  escaped* — is recorded alongside the main one, specifically to stop the next
  agent over-correcting.

## Tests

Two new suites, both **fully mocked and offline**: they `jest.mock`
`../src/lib/db` — the only DynamoDB client in the backend — plus
`../src/routes/customers`. No credentials, no network, nothing written to
production, and therefore **no `ZZTEST_` marker is needed**. That was verified,
not assumed.

- `backend/tests/item-notes.test.ts` — 31 tests
- `backend/tests/preorder-collection-time.test.ts` — 70 tests

## Verification

Re-run against the tree as it stands:

```
cd backend && npx tsc --noEmit    → exit 0, no output
cd backend && npm test            → 20 suites, 421 passed, 18 skipped, 439 total
npm run version:check             → all 6 markers agree (v1.73.0), SHELL complete
```

Baseline before this work: **18 suites, 320 passed, 18 skipped, 338 total**. So
**+2 suites, +101 tests, 0 regressions**.

Two caveats on that green run, stated because a green run on its own is not
evidence:

- **The 18 skips are a skip, not a pass.** They are `integration.test.ts`
  self-skipping without credentials and without `RUN_LIVE_WRITE_TESTS=1`. Neither
  it nor any Playwright journey was run in this pass — both write to the live
  production café.
- **13 source mutations were each confirmed to turn the suite red, then
  reverted.** `git diff backend/src` is byte-identical to the pre-mutation
  snapshot. This is what distinguishes "the tests pass" from "the tests have
  teeth".

## Release state — nothing has shipped

Explicitly, and by instruction:

- **Version NOT bumped.** The tree stays at **v1.73.0**; all six markers agree.
- **No `frontend/changelog.json` entry.**
- **Not committed, not pushed, not deployed.**
- **`frontend/sw.js` untouched** — no new `frontend/js/*.js` or
  `frontend/css/*.css` file was added, so the `SHELL` array needed nothing and
  `version:check` is clean.

The version bump and the changelog entry are a **later, separate pass**. Both
features are user-visible and will need a changelog entry when that pass happens.

When it does, note the deploy asymmetry that has bitten before: this work touches
**both** `backend/src/` and `frontend/`, and the backend must land **first**.
Until it does, `PUT /api/orders/{id}` rejects nothing new but `getOrder` returns
neither `collectionTime` nor `collectionOptions` — which is precisely why
`track.js` renders no picker when they are absent, rather than breaking.

## Follow-ups recorded, NOT fixed

Continuing the lettering; a and c–m above still stand, b is addressed.

n. **`frontend/prep.html` is not in the `sw.js` `SHELL` array.** Every other page
   is — verified: `./`, `index.html`, `track.html`, `pos.html`, `admin.html`,
   `display.html`, `reports.html`. So the **barista page is never precached and
   does not work offline**, on the one screen whose user is standing at a machine
   with a phone in a pocket. `npm run version:check` does not catch it because it
   only enforces `js/` and `css/` assets plus the versioned pages. Adding it
   changes caching behaviour on a live surface, so it should be a deliberate
   release decision rather than a side effect of this change.

o. **`generateCollectionSlots()` in `frontend/js/app.js` is dead code.** Defined
   and never called anywhere in the tree — grepped across `frontend`, `backend`,
   `scripts` and `docs`; the only hit is its own definition. It generates
   15-minute slots from 9:00 AM to 2:00 PM, which is **not** the option list
   actually used: the real list is the link's `collectionOptions`. It is a
   misleading second answer to "what collection times exist", sitting next to the
   code that uses the right one. **Correction to the brief:** it is at
   `frontend/js/app.js:80`, not `:50`.

p. **`frontend/js/pos-walkup.js:429` — `#wkNotes` still reads "Special requests
   (less sugar, extra hot)".** That wording has now moved to the per-item input on
   the customer page, so the cashier's **whole-order** box is using the per-item
   phrasing. Verified that this is now the only occurrence of that string in
   `frontend/`. A copy realignment and a decision for the café, not a bug.

q. **Process hazard — concurrent agents in one working tree must not `git stash`.**
   A bare `git stash` in this shared tree silently swallowed a parallel agent's
   uncommitted edits mid-task. They were recovered from the stash; `git stash
   list` is now empty and all work is present in the tree (both verified in this
   pass). `git stash` is repo-wide, not path-scoped, so it is never safe when
   another agent is editing. Use a scratch copy, or scope to explicit paths.

## Documentation and skills updated in this pass

- `.kiro/skills/api-reference/SKILL.md` — `POST /api/orders`,
  `GET /api/orders/{id}` and `PUT /api/orders/{id}` rows. No endpoint was added,
  removed or re-authed; all three remain existing public unauthenticated paths.
  The `PUT` row's claim that the backend "owns the notes prefix" was **incomplete
  rather than wrong** and is now stated precisely: the code is still
  backend-owned, the time is now customer-changeable within the link's list, and
  a validated time may create a prefix that did not exist.
- `.kiro/skills/db-schemas/SKILL.md` — `items` gains `note?` (80-char trimmed cap;
  written only when non-empty, so nothing needs backfilling); the `notes` row now
  says the time is customer-changeable on edit within `collectionOptions` with the
  code always taken from the stored record; and the pre-order code's
  `collectionOptions` row now says it is the **validation allowlist**, not merely a
  list of UI choices.
- `.kiro/skills/invariants/SKILL.md` — the new escaping bug class in **Frontend**
  (escape at every render site; the `textContent` counterpart; why the escapers
  are per-page-bundle), cross-referenced from the single-sources-of-truth list as
  its one accepted exception. Also extended **Create / edit parity** with
  `validateItemNote` / `resolveCollectionTime` and the "check whether create ever
  had the restriction" direction, and updated the "operational field never depends
  on the client re-sending it" item to say that backend-owned does not have to
  mean immutable. Frontmatter `description` updated to name escaping, collection
  times and item notes, and `.claude/skills/invariants/SKILL.md`'s stub
  description re-synced to match.
- `.kiro/skills/test-suites/SKILL.md` — both new suites added to the offline
  table, with an explicit note that they mock `lib/db`, need no credentials, write
  nothing to production and need no `ZZTEST_` marker.
- `docs/update-20260817.md` — this section, and follow-up (b) marked addressed.

### Checked and deliberately NOT changed

Each was read rather than assumed:

- `.kiro/skills/order-lifecycle/SKILL.md` — no status transition, guard or food
  counter effect changed. Grepped for collection-time and item-note references:
  none. A note and a collection time are inert with respect to the state machine.
- `.kiro/skills/pricing-rules/SKILL.md` — no money touched. Its only `collect`
  match is the phrase "what is actually collected" describing `totalAmount`, which
  is unrelated.
- `.kiro/steering/project.md` — no new `backend/src/routes/*.ts`,
  `backend/src/lib/*.ts` or `frontend/js/*.js` file. The layout block already
  lists `routes/orders` and `frontend/js` `app, track, pos*`.
- `.kiro/skills/release-flow/SKILL.md` and `.kiro/steering/release-checklist.md` —
  the release procedure itself is unchanged. Read both; the six markers, the
  `SHELL` rule and the two deploy paths are all still accurate as written.
- `frontend/sw.js` — nothing to add (see the release-state section above).
- `frontend/js/admin*.js` — a separate agent's escaper-consolidation work
  (follow-up (e) above) is uncommitted in this same tree. Left untouched.
- `docs/architecture.md`, `docs/requirements.md`, `docs/feature-history.md` — no
  contradiction. These are two item-level changes inside the existing pre-order
  and ordering features, not a new subsystem or a sprint-level feature; the
  sprint entry belongs with the release pass that ships them.

---

# Release pass — v1.74.0

> Also done on **2026-08-18**, appended here for the same reason as the section
> above: it closes out the T5 / follow-up-(b) thread rather than starting a new
> one. This is the pass the section above deferred ("the version bump and the
> changelog entry are a later, separate pass").

## What this pass did

No source behaviour changed. This pass bumped the version, wrote the
user-facing changelog, recorded the sprint entry, captured a UX audit that
existed nowhere on disk, and recorded three new follow-ups.

## The two XSS findings, side by side

The POS/`prep.html` finding is written up in **§3** above. The **admin** finding
was made independently, by a second agent working the same tree, and is recorded
here for the first time. Putting them next to each other is the point, because
they are **the same bug twice**:

| | POS / `prep.html` | Admin |
|---|---|---|
| Injected via | customer-typed `customerName`, item name/variant, `notes` | admin-typed labels (checklist, menu, ingredients, users, verses) |
| Sink | raw `innerHTML` interpolation | unquoted-escape `value="${…}"` attribute |
| Proven | `<img src=x onerror=…>` customer name **executed** | label `5" onmouseover="window.__XSS=1` produced **a real event handler that fired on hover** |
| Privilege | **CASHIER JWT** in `sessionStorage` | **ADMIN** session |
| Correct escaper already present? | **Yes** — `escapeHtmlPos` | **Yes** — `mfEsc` in `admin-menu.js` |
| Used correctly nearby? | **Yes** — the v1.71 dialogs escaped the same field | **Yes** — applied to option and group names at `admin-menu.js:238`/`:245` |

**The shared shape, stated once:** in both cases a correct escaper already
existed in the same file, was already being used correctly a few lines away, and
simply had never been applied to the **older** hot path. Neither bug was a
missing capability. Neither was a case of nobody knowing the rule. The newer code
got it right and the code it sat next to was never revisited.

This is why the invariant recorded in `.kiro/skills/invariants/SKILL.md` is
phrased as *escaping is a property of the render site, not of the field* — and
why the reviewable form of it is "grep every `innerHTML` and every `value="`
interpolation of this field", not "check that an escaper exists".

**Correction to follow-up (e).** As recorded at line 695 above, (e) judged this
"same-origin admin-authored text, so the realistic damage is a mangled form
rather than an XSS". **That was wrong, and understated it.** The verified
reproduction produces an executing event handler in an ADMIN session. (e) is now
addressed, but the mis-triage is worth keeping visible: "same-origin,
self-authored text" is not a reason to downgrade an injection, because the author
and the victim are not always the same person, and a stored label is replayed to
every later viewer.

### Admin escaper consolidation

Three byte-identical escaper copies collapsed into one canonical pair —
`escapeHtml` (`frontend/js/admin.js:79`) and `escapeAttr` (`:86`, a name that
reads correctly at an attribute; same escaping, all five of `& < > " '`). Two of
the three existed **only** to work around script ordering. `mfEsc` is gone
(verified: the only remaining matches for it in `frontend/js/admin*.js` are two
comments explaining its removal).

Worth recording because it is a latent trap rather than a tidy-up:
`admin-vouchers.js` loads at `admin.html:36`, **after most of its ~25 callers**,
and was relying on **cross-file function hoisting** to work at all. That happens
to be sound for `function` declarations at global scope, but it means the module
graph does not describe the real dependency, and any move to `defer`, modules or
a bundler would have broken it silently.

## The per-item note cap

`ITEM_NOTE_MAX_LENGTH = 80`, enforced by `validateItemNote` shared between
`createOrder` and `modifyOrder`. **Why 80 rather than the per-order 200:** a
per-order note renders **once**, an item note renders **once per line**, and the
binding constraint on both the barista prep card and the POS queue card is
vertical space. A four-drink order at 80 each keeps total free text in the same
order of magnitude as one order note. Measured on the **trimmed** value, so
trailing whitespace from a text input cannot push a legal note over. Full
rationale in §1 above; restated here so the release record carries the number and
the reason together.

## Version

**v1.73.0 → v1.74.0** — minor. Two user-visible features plus two security
fixes. Bumped with `npm run version:bump -- minor`, never by hand; all six
markers written in one pass.

Three changelog entries, volunteer-facing. The security line deliberately
**describes the hardening, not the exploit** — "unusual characters (quotes, angle
brackets) can no longer disturb the page" — because `changelog.json` renders in
the "What's new?" modal on admin/reports and is read by volunteers, and because
the fix ships to GitHub Pages before every cached service-worker shell has
updated.

**`frontend/sw.js` `SHELL` needed no addition.** No new `frontend/js/*.js` or
`frontend/css/*.css` file was created in this release — every frontend change was
a modification to a file already listed. `version:check` is clean.

## Verification (this pass)

```
cd backend && npx tsc --noEmit    → exit 0, no output
cd backend && npm test            → 20 suites, 421 passed, 18 skipped, 439 total
npm run version:check             → all 6 markers agree (v1.74.0), SHELL complete
```

Re-run independently in this pass rather than carried over from the section
above.

- **The 18 skips are skips, not passes.** `integration.test.ts` self-skipping:
  `describeAuthed`/`describeLiveWrites` resolve to `describe.skip` without
  credentials and without `RUN_LIVE_WRITE_TESTS=1` (`tests/integration.test.ts:24`,
  `:44-45`). Confirmed in the file, not assumed from the count.
- **Neither the live integration suite nor any Playwright journey was run**, by
  instruction. Both write to the live production café. A live Order Flow run on a
  Sunday afternoon would additionally write a `DAILY_SUMMARY#{date}` marker and
  thereby **suppress the genuine end-of-day summary email** — the exactly-once
  guard added in v1.72.0 cannot tell a test run from the real one.
- **No production writes of any kind** in this pass, so no `ZZTEST_` records
  exist and `scripts/cleanup-test-data.mjs` was not needed.
- `tmp/` and `.impeccable/` confirmed gitignored (`.gitignore:33`, `:39`) and
  **not staged**. `tmp/pos-mock.html` is being written by another agent
  concurrently and was not touched.

## UX audit recorded — `docs/pos-ux-audit-20260818.md`

An `/impeccable critique` of the POS scored it **20/40, "acceptable, bottom
edge"**, weakest on **Visibility of System Status (1/4)** and **Error Recovery
(1/4)**. It had produced **no artefact on disk** and would have been lost, so it
is now written up in full.

Three method caveats are recorded at the top of that file rather than buried,
because each is a reason a finding could be wrong: the café was OPEN with an
**empty queue** so nearly all rush states were **SIMULATED via intercepted GET
responses** (**zero writes**); the mechanical detector ran **degraded** on a
regex fallback so its counts are an **undercount**; and it was **Chromium at
iPad viewports, not real iOS Safari**, so touch-target and portrait-breakpoint
findings are unconfirmed on the actual counter hardware.

Its headline P0 is that **a failed fetch renders an empty board indistinguishable
from "no orders"**: `renderBoard()` sits inside the `try` (`pos.js:385`), the
`catch` only toasts (`:408`), the toast auto-hides after 3s (`:37`) against a 7s
poll (`:331`) — so the only warning is visible **≈43%** of the time, over a green
OPEN badge. Its second P0 is the XSS, fixed in this release.

**One line-number caveat on that document.** The audit ran against the tree
*before* this release's `pos.js` and `style.css` edits, so its original
references had drifted by up to 11 lines (`askStaffPrice` :856→:865,
`confirmReleaseNotToday` :973→:982, `releaseAllPreorders` :915→:911, and every
`style.css` reference). **All references in the written-up file were re-resolved
against the tree at v1.74.0** and the three contrast ratios recomputed from the
CSS custom properties (2.95:1, 3.85:1, 2.54:1 — all reproduced exactly).

It also carries an **open product question** that should be settled before any
further kanban polish: for one volunteer on one tablet doing one thing at a time,
is a single FIFO list better than three columns of which two are always mostly
off-screen? A preview mock is being built at `tmp/pos-mock.html` (gitignored,
deliberately not part of this release).

## Follow-ups — new in this pass

Continuing the lettering. **(e) is now addressed** (admin escaping, above).
**(n), (o) and (p) still stand exactly as written** — `prep.html` missing from
the `SHELL` array, the dead `generateCollectionSlots()`, and the `#wkNotes`
placeholder wording. They are **not** re-lettered here; all three were
re-verified against the tree in this pass and restating them would create a
second copy to keep in sync.

Re-verified, briefly: `prep.html` is absent from `sw.js`'s `SHELL`
(`frontend/sw.js:2-13`) and `version:check` cannot catch it because the script
only enforces `js/`, `css/` and the versioned pages — and nothing in `frontend/`
links to `prep.html` at all, so it is orphaned as well as unprecached; adding it
changes caching behaviour on a live surface, so it stays a deliberate release
decision. `generateCollectionSlots()` is still the only match for its own name
(`frontend/js/app.js:80`). `pos-walkup.js:429` still reads "Special requests
(less sugar, extra hot)".

r. **`frontend/js/admin.js:679-908` — `loadReports` / `renderReportsSection` are
   231 dead lines holding ~10 unescaped interpolations.** Verified: `loadReports`
   has **no caller anywhere** in the tree — the only match for the name is its own
   definition at `:679`. The live path is `loadReportsTab` (`:202`), which delegates
   to `window.RLCReports.mount(container)` (`reports.js:702`). The block ends at
   `:908`, immediately before `// --- Helpers ---`.

   **This one is a trap, which is why it is recorded rather than fixed here.**
   Someone auditing escaping will find those ~10 interpolations, fix them
   carefully, verify by *reading* the code, and ship **nothing** — the lines never
   execute. Worse, doing so makes the dead code look maintained and pushes the
   next reader to the same wasted effort. It should be **deleted**, and as its
   own reviewable change: a 231-line deletion mixed into a security release is
   unreviewable, and deleting it *is* the fix for the interpolations.

s. **`frontend/js/reports.js` defines two escapers internally AND shadows the
   canonical one.** `escapeHtml` at `reports.js:64` and `escapeHtmlReport` at
   `:470`. Meanwhile `reports.js` loads at **`admin.html:45`, after every admin
   module** (`admin.js` is `:31`), so on the admin page its global `escapeHtml`
   **overwrites** the canonical `admin.js:79` one for every later caller.

   Harmless **today** because the implementations are identical — which is exactly
   what makes it dangerous: the moment either is edited, admin-page escaping
   silently changes behaviour with no error, no warning and nothing in the diff to
   suggest the two files interact. Directly undercuts the consolidation done in
   this release, so it should be closed out before the next escaping change.

t. **`frontend/js/admin-dashboard.js:224` and `:233` interpolate a drink name
   unescaped** into the discount accordion: `` `${name} ×${qty}` `` at `:224`,
   joined into `drinkText` and written raw at `:233`
   (`<td colspan="3">${drinkText || '—'}</td>`). Menu-item names are
   admin-authored, so this is lower severity than the two fixed above — but it is
   **the same class of bug in the same release's blast radius**, and per the
   mis-triage of (e) recorded earlier in this section, "admin-authored" is not a
   reason to leave it. `escapeHtml` is already in scope from `admin.js`; the fix
   is one call at each site.

   **CLOSED in v1.75.0** — `admin-dashboard.js:233` now interpolates
   `escapeHtml(name)`, with a comment recording why an admin-entered string is
   escaped anyway (every render site gets escaped, not just the ones with a scary
   source). Verified in the tree.

## Documentation updated in this pass

- `frontend/changelog.json` — v1.74.0 entry, three volunteer-facing lines
  (written by `bump-version.mjs`, not by hand).
- `docs/feature-history.md` — new **2026-08-18 Sprint — v1.74.0** entry. This is
  the release that ships T5 and follow-up (b), so the sprint-level entry the
  previous section deferred belongs here.
- `docs/pos-ux-audit-20260818.md` — **new file**, the full audit write-up
  described above.
- `docs/update-20260817.md` — this section; (e) marked addressed with its
  mis-triage corrected; follow-ups r, s and t added.

### Checked and deliberately NOT changed

- `.kiro/skills/api-reference/SKILL.md`, `db-schemas`, `invariants`,
  `test-suites` — all four were already updated by the implementation pass (see
  the previous section's list) and were re-read here rather than re-edited. No
  endpoint, table, attribute, marker or invariant changed in the release pass
  itself.
- `.kiro/skills/order-lifecycle/SKILL.md`, `pricing-rules` — no status
  transition, guard, food-counter effect, price or discount rule touched.
- `.kiro/skills/release-flow/SKILL.md` and
  `.kiro/steering/release-checklist.md` — **the release procedure itself did not
  change.** Both were read in full at the start of this pass; the six markers,
  the `SHELL` rule, the pre-deploy list and the two asymmetric deploy paths are
  all still accurate. Nothing to correct, so nothing was edited.
- `.kiro/steering/project.md` — no new `backend/src/routes/*.ts`,
  `backend/src/lib/*.ts` or `frontend/js/*.js` file in this release. The layout
  block already covers every file touched.
- **No new skill created.** The UX audit is a point-in-time finding set with an
  unresolved product question, not a stable subsystem reference. Making it a
  skill would put a 20/40 snapshot — with simulated states and a degraded
  detector — into the always-loadable set, where a future agent could read it as
  current fact. It belongs in `docs/`, dated. Revisit if the FIFO-vs-kanban
  question is decided and a durable POS-surface convention comes out of it.
- `docs/architecture.md`, `docs/requirements.md` — no contradiction. No new
  component, data flow or requirement; the UX audit records defects against the
  existing design rather than changing it.

## Still for the user to run — v1.74.0

**Nothing has been pushed or deployed.** Committed to `master` only.

This release touches **both** `backend/src/` and `frontend/`, so **order
matters**:

```bash
npm run deploy:backend     # FIRST — cdk deploy → ap-southeast-5, acct 956288449190
npm run deploy:frontend    # THEN — git push; Actions publishes Pages
```

- **Backend first, and it is not optional here.** `PUT /api/orders/{id}` must be
  validating `collectionTime` against `collectionOptions`, and `getOrder` must be
  returning `collectionTime`/`collectionOptions`, before the frontend offers the
  picker. The frontend is written to degrade safely in the meantime —
  `track.js` renders no picker when both fields are absent — so a frontend-first
  deploy is not *broken*, just featureless. Item notes are backwards-compatible in
  both directions: `note` is attached only when non-empty.
- **`deploy:frontend` runs `version:check` as `predeploy:frontend`.** It is clean
  as committed.
- **The Pages workflow only triggers on pushes to `master` touching
  `frontend/**`.** This commit does touch `frontend/`, so the push will publish.
  Note the corollary for later: a follow-up commit fixing only `docs/` or
  `backend/` will **not** republish the frontend.
- **Old service-worker shells stay live until they update.** `CACHE_NAME` is now
  `rlc-cafe-v1.74.0`, so clients re-fetch the shell on next activation, but a
  cashier mid-service may still be on the v1.73.0 shell — which is why no API
  response shape was changed in a backwards-incompatible way.
- **Optional, and only with the café OPEN:** `backend/tests/integration.test.ts`
  and the Playwright journeys. Both write real production records. **Avoid a
  Sunday-afternoon Order Flow run** — it writes `DAILY_SUMMARY#{date}` and
  suppresses the genuine summary email. Clean up with
  `scripts/cleanup-test-data.mjs`; see the `test-suites` skill first.
- **Decide the FIFO-vs-kanban question** in the UX audit before commissioning any
  further kanban styling work, and take a look at `tmp/pos-mock.html` when the
  other agent has finished it.

---

# v1.75.0 — POS cashier board overhaul and admin fixes (work done 2026-08-18)

> Appended to this file rather than split out, for the same reason as the
> sections above: it continues the thread. Specifically it **answers the open
> product question** the UX audit left (FIFO vs kanban) and **closes follow-ups
> (j) and (t)**. Read the date headings, not the filename.
>
> **v1.74.0 (`f36d4e9`) was committed but never pushed or deployed.** v1.75.0
> stacks on top of it in the same working tree, so **one deploy ships both** — the
> "Still for the user to run" section at the very bottom supersedes v1.74.0's.

## ⛔ Process failure first — a verification probe wrote to PRODUCTION

This is recorded before the feature work because it is the most important thing in
this session, and because it will otherwise be repeated.

**What happened.** During the admin pass, a Playwright verification harness that
was believed to be read-only **flipped `latte-001` (☕ Latte) to
`isActive:false`** — taking Latte **off the live customer menu**. It has since
been restored to `isActive:true, isEnabledToday:false` and verified.

**Why the guard failed.** The harness blocked writes with `page.route()`, matching
all non-`GET` requests. **`page.route()` does not intercept requests issued by a
service worker**, and *every page in this app registers one* — `frontend/sw.js`'s
`fetch` handler passes `/api/` requests straight to the network. So the admin
page's `PUT` never passed through the interceptor at all. The block did not fail
noisily; it simply was never consulted.

**The tell, and it was there.** A positive control reported **0 allowed
requests**. A route handler that has intercepted *nothing* is not a handler that
blocked everything — it is a handler that was never in the path. Zero
interceptions on a page that is visibly loading data is a red flag, not a clean
bill of health.

**The fix, both halves required:**

```js
const ctx = await browser.newContext({ serviceWorkers: 'block' });   // context-level; too late on an existing page
let seen = 0;
await ctx.route('**/api/**', r => { seen++; return r.continue(); }); // POSITIVE CONTROL
// …after navigating and interacting:
if (seen === 0) throw new Error('interception never fired — assume writes escaped');
```

**Second-order consequence, which matters more than the single record.** The
**earlier admin UX audit's "read-only" claim was asserted with the same defective
mechanism**, as is the `journey_admin` "read-only" row in the `test-suites` skill.
Neither is evidence of anything. Both are now marked unproven rather than quietly
left standing — a false safety claim is worse than no claim, because it is the one
a future agent will rely on.

Written up in **`.kiro/skills/test-suites/SKILL.md`** as a fenced box at the top
of the file (not a footnote), with the frontmatter `description` extended so the
skill is actually found by an agent about to drive a browser against the live API.

**Not covered by the `ZZTEST_` marker rule.** The prefix protects *created*
records. This was a **mutation of a real catalogue record**, which no marker and
no `scripts/cleanup-test-data.mjs` sweep would ever have found or reverted. Worth
knowing: the cleanup script is not a safety net for probe writes.

## Context

Two design passes plus one fix, **all frontend — no `backend/src/` file changed in
this release.** Both passes were built from mocks the user previewed and approved
before any production code was written.

## The two design decisions, and the reasoning worth keeping

### 1 — The three-lane kanban stays, repaired. Single-FIFO-list was rejected

The UX audit (`docs/pos-ux-audit-20260818.md`) closed with an open product
question: for one volunteer on one tablet doing one thing at a time, is a single
FIFO list better than three columns of which two are always mostly off-screen? A
mock of both was previewed.

**The user chose the repaired three-lane kanban and explicitly rejected the single
FIFO list, because an order that is placed and never paid would permanently occupy
the first row.** In this café that is not a hypothetical: PENDING orders wait on a
receipt upload, some never arrive, and the 1-hour expiry sweep is the only thing
that eventually clears them. A single list sorted by age therefore parks its most
prominent, most-glanced-at row on the one order nobody can act on, and every real
order is pushed down by it. Three lanes keep unpayable orders quarantined in
Pending where they cost nothing.

**This reasoning is durable and will come up again** — the next person to look at
this board will independently notice that two lanes are usually near-empty and
propose exactly the same simplification. The answer is not "we prefer columns";
it is "a status-blind ordering puts a dead order in the position of highest
attention". Any future single-list proposal has to solve that first.

### 2 — Name both menu flags per row, rather than a separate today-only screen

The admin Menu tab was editing **two different flags with one visual language**
(details below). Two fixes were mocked: name both flags on every row, or split
"serving today" onto its own screen. **The user chose naming both flags per row.**
The split would have made the safe daily operation a separate destination while
leaving the permanent catalogue switch as the default thing under the operator's
thumb — the more dangerous flag, easier to reach.

## Changes

### POS cashier board — `frontend/js/pos.js`, `frontend/js/pos-checklist.js`, `frontend/css/style.css`, `frontend/pos.html`

- **Queue order, from ONE shared comparator: receipt-first, then oldest-first.**
  Previously Pending sorted **newest-first**, Preparing and Ready had **no sort at
  all**, and the list view sorted differently again. A 17-minute order that had
  already fired the urgent chime rendered **sixth, ~1100px below the fold** — the
  board answered the exact opposite of "who has waited longest", which is its
  whole job. One comparator now serves every lane and the list view, so they
  cannot drift apart again.
- **A persistent stale / connection-lost state** — the audit's headline P0. A
  failed fetch used to render an **empty board under a green OPEN badge**,
  indistinguishable from "no orders", warned only by a toast that auto-hid after
  3s against a 7s poll (present ≈43% of the time). Now: the lanes always render,
  the header badge flips to **"LAST SEEN OPEN · &lt;time&gt;"**, a persistent panel
  states the data's age and offers **Retry**, and mutating controls go **inert**
  rather than acting on figures known to be stale.
- Sticky lane headers with per-lane scroll; `minmax(0,1fr)` so one long customer
  name can no longer crush the other two lanes to ~110px.
- **A real `:disabled` style**, generalised from the single
  `.pos-btn-preorder-release[disabled]` rule that already existed. Disabled
  primaries had been rendering as a full brown gradient with `cursor:pointer`.
- Contrast: lane headers 2.95:1 → **6.46:1**, 3.85:1 → **6.77:1**, 4.75:1 →
  **8.01:1**; wait time 2.54:1 → **6.89:1**. Every `#9CA3AF` removed.
- Approve and Ready differentiated by colour. Card hierarchy lifted so the drinks
  are no longer the quietest text — **except on Ready cards, where the customer
  name is promoted instead**, because the job there is calling a name across a
  room. Deliberate asymmetry, not an oversight.
- **Sidebar docked at ≥900px.** It had stayed off-screen at *every* width, so the
  empty state's "Tap Open to start service" named a button at `left:-212px`.
- Stats strip visible by default with receipt/pre-order flags; a teaching empty
  state; the list view no longer snaps back to the Pending tab every 7s; the
  sidebar status dot no longer contradicts the header badge; **Logout guarded** —
  it had been one unguarded tap mid-service — via a new in-app **`posConfirm()`**
  (`pos.js:1385`) rather than `window.confirm`.
- **Opening checklist made completable** (`pos-checklist.js`): "N of M done"
  counter, progress bar, an explicit "N more below" affordance, a submit label
  reading "☀️ Open Café — 4 items left", and tapping it while blocked **scrolls to
  and highlights the first unchecked row** instead of doing nothing. The 12-item
  list had shown **8**, and the four below the fold included "Enable menu items &
  food quantities in POS". Plus a guard so an **empty checklist config** can no
  longer disable Open Café forever.
- **Zero touch targets under 44px.**

### Admin — `frontend/js/admin*.js`, `frontend/css/admin.css`

- **The Menu tab had been editing two different flags with one visual language.**
  The row switch writes `isActive` (permanent catalogue); the bulk buttons write
  `isEnabledToday` (today only); and every badge, count and filter drew
  `isActive` **alone**. Consequences: **"Disable All" produced zero visible change
  and no toast**, and a filter reading "Enabled Only (32)" counted *catalogue*
  items while the operator read it as "32 serving today". Now both flags are
  **named and drawn per row** with state pills and a "why" line, the summary line
  is corrected, there are **six symmetric bulk actions** each with confirmation
  and a result toast, and **Delete is demoted** (`admin-danger-quiet`) **and
  confirmed by name** (`admin-menu.js:129`, `:284`).
- **A leave guard with a change count.** There had been **no dirty-state tracking
  anywhere in the admin**, so switching tabs silently discarded edits. One
  registry, applied to **Checklist and Settings** and structured so other tabs can
  opt in. **Reverting an edit puts the guard back to sleep** — a count that only
  ever goes up trains people to dismiss it.
- Sidebar: all **14** destinations reachable at 1024×768. Logout had been **245px
  below the fold with no scroll cue**.
- Disabled item rows **tinted instead of faded**: item name 2.20:1 → **5.62:1**,
  subtitle 2.29:1 → 5.62:1, `badge-food` 2.86:1 → 6.46:1, Logout 4.63:1 →
  **8.80:1**.
- Checklist: label on its own line (~110px of visible text across 34 items
  before), sticky save bar with a live count, two-tap Remove. **Drag and ▲▼
  reordering untouched.**
- **Zero touch targets under 44px.** `.toggle-switch` had been 42×24 across ~130
  instances; the label is now the hit area and clears `--tap` in both axes while
  the switch art stays small (`admin.css:324-335`).
- **Fix: "today" was derived from UTC in 8 places**, so before 08:00 MYT the admin
  was a day behind and `admin-preorder.js` could stamp `serviceDate` as
  **Saturday**. One helper — **`mytToday()` at `admin.js:109`**, with
  `isoAddDays`, `isoDayOfWeek` and `mytDayLabel` beside it — is now used at every
  site, including `computePastSundays` (`admin-dashboard.js:27`). **Closes
  follow-up (j).** The original bug there was self-contradictory: **local**
  `getDay()` combined with **UTC** serialisation.
- Keyboard-operable sortable `<th>` with `aria-sort`; accordion with
  `aria-expanded`.
- **Closes follow-up (t)** — the unescaped drink name in the dashboard discount
  accordion, now `escapeHtml(name)` at `admin-dashboard.js:233`.
- **New caller of an existing endpoint.** The row's today-switch calls
  **`PUT /api/pos/menu/{id}/toggle`** (`admin-menu.js:272`), chosen over the
  generic admin PUT because that POS route **also flags PENDING orders containing
  the item**. Verified present at `backend/src/routes/pos.ts:1455-1457`
  (`toggleMenuItem`, `:1115`). **No endpoint, method, auth requirement or schema
  changed** — hence no `api-reference` or `db-schemas` edit; both already document
  the route (`api-reference` line 57) and both flags (`db-schemas` lines 54-55).

### One fix made directly during the release pass

- **`.pos-cl-fill` was animating `width`**, relayouting every frame, which janks
  on the counter iPad. It now scales an always-full-width fill:
  `transform:scaleX(var(--cl-progress,0))` from `transform-origin:left center`,
  with the JS setting the `--cl-progress` custom property (0–1) rather than a
  percentage width (`style.css:2570-2582`, `pos-checklist.js:94`). Plus a
  `@media(prefers-reduced-motion:reduce)` escape, and the track gained
  `role="progressbar"` with `aria-valuenow`/`min`/`max`.

## Verification (this pass)

Re-run independently in this pass rather than carried over.

```
cd backend && npx tsc --noEmit    → exit 0, no output
cd backend && npm test            → 20 suites, 421 passed, 18 skipped, 439 total
npm run version:check             → all 6 markers agree (v1.75.0), SHELL complete
```

- **The 18 skips are skips, not passes.** `integration.test.ts` self-skipping;
  `describeAuthed` / `describeLiveWrites` resolve to `describe.skip` without
  credentials and without `RUN_LIVE_WRITE_TESTS=1`.
- **No backend source file changed in this release**, so `tsc` and `npm test` are
  a regression check on an unchanged backend rather than coverage of this work.
  The frontend has no automated tests here; the POS and admin changes are
  **unverified by any suite** and were reviewed by reading the diff.
- **Neither the live integration suite nor any Playwright journey was run in this
  pass**, by instruction — and see the process-failure section above for why the
  bar for "safe browser probe" is now higher than it was.
- **The `latte-001` restoration was performed and verified before this pass** and
  is recorded here as reported; it was **not** re-read from production here, since
  that would mean live credentials against the production table. If any doubt
  remains, check the Menu tab: Latte should read *on the menu, not serving today*.
- 14 modified frontend files, no new file — hence **no `SHELL` addition needed**,
  which `version:check` confirms independently.
- `tmp/` and `.impeccable/` confirmed gitignored and **not staged**.
- No credential, PIN or token in the diff.
- **Contrast ratios and touch-target counts in this document are as reported by
  the design passes.** They were not independently recomputed in this pass; the
  CSS changes backing them were read.

## Follow-ups

### Closed by this release

- **(j) — CLOSED.** `computePastSundays` and every other admin "today" now goes
  through `mytToday()`.
- **(t) — CLOSED.** `admin-dashboard.js:233` escapes the drink name.

### Downgraded

- **(a) — still open, but it now only bites below 900px.** The `margin-left`
  transition on `.admin-main` / `.pos-main` is unchanged, but **the sidebar is
  docked and static at ≥900px**, so on the counter iPad in landscape — the width
  that matters — there is no longer a sliding transition to jank. It remains a
  real defect on phone widths and in portrait. Downgraded, not fixed.

### Still open, unchanged

- **(n), (o), (p)** — `prep.html` missing from the `SHELL` array; the dead
  `generateCollectionSlots()`; the `#wkNotes` placeholder wording.
- **(r)** — `loadReports` / `renderReportsSection`, ~231 dead lines with ~10
  unescaped interpolations. Still uncalled: `loadReports` has no caller anywhere in
  the tree, and the live path is still `loadReportsTab` →
  `window.RLCReports.mount()`. Still wants its own reviewable deletion.
  **Line numbers re-resolved against this release's `admin.js`, which grew by the
  MYT helper and the unsaved guard: the block is now `:958-1188`** (`loadReports`
  at `:958`, `renderReportsSection` at `:973`, ending immediately before
  `// --- Helpers ---` at `:1189`), not the `:679-908` recorded earlier in this
  file. A 279-line drift, which is exactly how a follow-up rots.
- **(s)** — `reports.js` defines its own `escapeHtml` and loads at
  `admin.html:45`, *after* `admin.js` at `:31`, so on the admin page it **shadows
  the canonical escaper** for every later caller. Re-verified in this pass. Now
  noted in the `invariants` skill beside the consolidation it undercuts.
- **(c), (d), (f), (g), (h), (i), (k), (l), (m), (q)** — unchanged.

### New

u. **Delete is now inconsistent *because* Menu was fixed.** Menu's Delete is
   quiet (`admin-danger-quiet`) and names the item in its confirm
   (`admin-menu.js:129`, `:284`). Everywhere else it is still solid red
   `pos-btn-danger` with an **unnamed** `window.confirm`: Ingredients
   (`admin-ingredients.js:70`, `:121` — "Delete this ingredient?", and the
   ingredient list is the longest in the admin, so this is the highest-exposure
   one), Users (`admin.js:507` — "Delete this user?"), Verses
   (`admin-verses.js:54`), Vouchers (`admin-vouchers.js:337`, `:355` — Revoke,
   "cannot be undone", unnamed), Pre-order (`admin-preorder.js:82`, `:108` —
   Deactivate; this one *does* name the code) and Display
   (`admin-display.js:71`, `:124` — names no slide). The inconsistency existed
   before, but fixing one tab has made it **more visible, not less**, and an
   operator who learns "Delete asks me to confirm the name" is now wrong on six
   tabs out of seven. All of these also still use `window.confirm` rather than the
   new in-app `posConfirm()`.

v. **`.check-update-btn` is still sub-44px on `index.html` only — narrower than
   reported.** Verified rather than accepted: `pos.html` is covered by
   `body.pos-shell .check-update-btn{min-height:var(--tap)}`
   (`style.css:1212-1214`), and **`reports.html` and `admin.html` are covered by
   the unscoped `.check-update-btn{min-height:var(--tap)}` in `admin.css:154`,
   which both pages load after `style.css`**. That leaves the base rule at
   `style.css:3118` applying only to `index.html`, the customer page, which loads
   `style.css` alone. Also note **`.app-version` is a non-interactive `<span>`**,
   so the 44px floor does not apply to it at all — the interactive neighbour is
   the button. Low severity: one underlined text button in a footer on the
   customer page.

w. **The frontend has no automated test of any kind.** Two full design passes over
   the POS board and the admin shipped in this release with **zero test coverage** —
   the shared queue comparator, the stale-state transition, the dirty-state
   registry and `mytToday()` are all pure functions or near enough, and all four
   are exactly the kind of logic that drifts silently. The comparator in
   particular is now load-bearing for three lanes plus the list view. Worth a
   small offline harness before the next POS change, precisely *because* the only
   verification mechanism available today is a browser probe — and the process
   failure above shows what that costs.

## Documentation and skills updated in this pass

- `frontend/changelog.json` — v1.75.0 entry, six volunteer-facing lines (written
  by `bump-version.mjs`, not by hand).
- `docs/feature-history.md` — new **2026-08-18 Sprint — v1.75.0** entry; the
  "Current Status (as of …)" date advanced to 2026-08-18.
- `docs/update-20260817.md` — this section; (j) and (t) marked closed, (a)
  downgraded, follow-ups u, v and w added.
- **`.kiro/skills/test-suites/SKILL.md`** — the service-worker / `page.route()`
  hazard as a fenced box at the top of the file, the `serviceWorkers:'block'`
  plus positive-control harness rule, the `journey_admin` "read-only" row marked
  **unproven**, a new category-2 row for ad-hoc browser probes, a "Writing tests"
  bullet, and an extended frontmatter `description` so the skill is found by an
  agent about to open a browser.
- **`.kiro/skills/invariants/SKILL.md`** — four corrections and additions:
  1. **`mfEsc()` text corrected.** It said the admin uses `mfEsc()`; that has been
     wrong since v1.74.0. `escapeHtml` / `escapeAttr` in `admin.js` (`:79`, `:86`)
     are canonical for the whole `admin*.js` bundle and `mfEsc` no longer exists
     (both remaining matches are comments recording its removal). Corrected in
     **both** places it appeared — the do-not-duplicate exception near the top and
     the Frontend rule — and follow-up (s)'s shadowing hazard noted beside it.
  2. **`mytToday()` added to the single-sources-of-truth table** as the admin
     frontend's Malaysia-time date authority, with the rule that spells out the
     original bug: decide the day in MYT, *then* do plain UTC arithmetic on the
     `YYYY-MM-DD` string — never mix the two.
  3. **Four new frontend invariants**, each carrying the production bug that
     motivated it: a failure state must not render identically to an empty success
     state; two distinct persisted flags must not share one visual language; a
     `[disabled]` control must look disabled; animate `transform`/`opacity` and
     never a layout property.
  4. Frontmatter `description` extended to name all of the above.

### Checked and deliberately NOT changed

- `.kiro/skills/api-reference/SKILL.md` — **no endpoint, method or auth
  requirement changed.** The admin gained a *caller* of
  `PUT /api/pos/menu/{id}/toggle`, which is already documented (line 57) with the
  correct path and auth. Verified in `backend/src/routes/pos.ts:1455` before
  deciding not to edit.
- `.kiro/skills/db-schemas/SKILL.md` — no table, GSI, TTL or attribute changed.
  `isActive` and `isEnabledToday` are both already documented on the menu table
  (lines 54-55) and their descriptions are accurate; this release changed how the
  **UI** presents them, not what they are.
- `.kiro/skills/order-lifecycle/SKILL.md` — no status transition, guard or
  food-counter effect touched. The board re-**orders** and re-**renders** orders;
  it does not change what any transition does.
- `.kiro/skills/pricing-rules/SKILL.md` — no price or discount rule touched.
- `.kiro/skills/release-flow/SKILL.md` and
  `.kiro/steering/release-checklist.md` — **the release procedure itself did not
  change.** Both read in full at the start of this pass; the six markers, the
  `SHELL` rule, the pre-deploy list and the two asymmetric deploy paths are all
  still accurate.
- `.kiro/steering/project.md` — **no new `backend/src/routes/*.ts`,
  `backend/src/lib/*.ts` or `frontend/js/*.js` file.** All 14 modified files
  already appear in the layout block. `mytToday()` and `posConfirm()` are new
  *functions* in existing files, recorded in `invariants` rather than in the
  layout block.
- **No new skill created.** The POS board and the admin Menu tab are surfaces
  covered by existing references; the durable lessons from this release are single
  invariants and one test-safety rule, and each went to the skill that already
  owns its subject. A "pos-board" skill would be a snapshot of a design that is
  still settling.
- `docs/architecture.md`, `docs/requirements.md` — no contradiction. No new
  component, data flow or requirement; this release fixes how existing data is
  presented.
- `docs/pos-ux-audit-20260818.md` — **left as the dated point-in-time record it
  is.** Its P0s are now fixed and its open product question is now answered, but
  rewriting a dated audit to match today's tree destroys the evidence of what was
  found and when. The answer to its product question is recorded **here**, in this
  release's notes, where it belongs. Its "read-only" method claim is, however,
  now contradicted by the process failure above — treat that document's admin
  findings as unproven.

## Still for the user to run — v1.74.0 **and** v1.75.0 together

**Nothing has been pushed or deployed.** Two commits on `master` only: `f36d4e9`
(v1.74.0) and this one. **This section supersedes the v1.74.0 one above** — one
push and one CDK deploy ship both.

```bash
npm run deploy:backend     # FIRST — cdk deploy → ap-southeast-5, acct 956288449190
npm run deploy:frontend    # THEN — git push; Actions publishes Pages
```

- **Backend first, and still not optional** — because of **v1.74.0**, not this
  release. `PUT /api/orders/{id}` must be validating `collectionTime` against
  `collectionOptions`, and `getOrder` must be returning
  `collectionTime`/`collectionOptions`, before the frontend offers the picker.
  v1.75.0 itself needs **no backend change**: `PUT /api/pos/menu/{id}/toggle` has
  been live for a long time.
- **`deploy:frontend` runs `version:check` as `predeploy:frontend`.** Clean as
  committed (v1.75.0).
- **The Pages workflow only triggers on pushes to `master` touching
  `frontend/**`.** Both commits touch `frontend/`, so the push will publish.
- **Old service-worker shells stay live until they update.** `CACHE_NAME` is now
  `rlc-cafe-v1.75.0`; a cashier mid-service may still be on the v1.73.0 shell, so
  no API response shape was changed incompatibly in either release.
- **Worth a look immediately after the frontend publishes**, because none of it is
  covered by a test: the POS board at the counter iPad's real width (lane order,
  the docked sidebar at ≥900px), the opening checklist counter, and the admin Menu
  tab's two switches. Pull to refresh, or use **Check for update** in the footer,
  to make sure you are on the v1.75.0 shell first.
- **Confirm ☕ Latte reads *on the menu, not serving today*** in Admin > Menu —
  the restored state after the probe write described at the top of this section.
  It should also be **absent from today's customer menu** until someone enables it
  for the day, which is the correct pre-service state.
- **Optional, and only with the café OPEN:** `backend/tests/integration.test.ts`
  and the Playwright journeys. Both write real production records. **Avoid a
  Sunday-afternoon Order Flow run** — it writes `DAILY_SUMMARY#{date}` and
  suppresses the genuine summary email. Clean up with
  `scripts/cleanup-test-data.mjs`; read the `test-suites` skill first, **including
  its new service-worker box**.

---

# v1.75.1 — payment is QR-ONLY, and the app had it wrong in three places (work done 2026-08-18)

> **Dated 2026-08-17 by filename, work done 2026-08-18.** Appended here rather
> than split into `docs/update-20260818.md`, continuing the convention set by the
> v1.74.0 / v1.75.0 sections above. Read the date in this heading.

Third release of the day, on top of **v1.75.0, which is already pushed and
deployed**. So this is a clean follow-on, not another stacked unpushed release.
**Frontend only — no `backend/src/` file changed.**

## The corrected domain fact — read this first

The user corrected a fact the app had been asserting wrongly. Their words:

> **"Only QR. No card, no cash"** — **"The QR is on the table"**

So, in full:

- **Payment is QR-ONLY.** No cash, no card, no other method.
- **The QR is PHYSICAL** — a Maybank DuitNow QR **printed on the café tabletops**.
- **The app renders no QR of its own, and never should.**
- **Therefore nothing can be paid at the counter.** There is no till transaction
  to perform. What the counter is for is **proving** a payment already made.

Every one of those four sentences was contradicted somewhere in the shipped tree.

**Why this is recorded so prominently: the wrong assumption had been encoded
independently in five places**, three of them user-visible and two of them
instructions to whoever came next —

| Where | What it said | Kind |
|---|---|---|
| `frontend/js/track.js` | a filled **"Pay at the counter"** card as the primary payment method | user-visible |
| `frontend/js/app.js` | cart footer `🏪 Pay at the counter after ordering` | user-visible |
| `frontend/js/pos.js` | walk-up tag `🚶 walk-up · cash at counter` | user-visible |
| `frontend/js/track.js` (comment) | 15 commented-out lines of in-app QR, "re-enable once real payment details are in place" | invitation to build it |
| `frontend/img/README.md` | *"Place `qr-payment.png` here — this is the Maybank DuitNow QR code shown to customers on the tracking page."* | invitation to build it |

A fact that has to be corrected in five places was never written down anywhere as
a fact. It is now: `.kiro/steering/project.md` **Operating context**, and as a
reviewable assertion in the `invariants` skill.

## Analysis

### 1. It was never a choice between two payment methods

The tracking page presented **"Pay at the counter"** as a filled primary card with
the amount in it, and **"Or show payment to cashier at counter →"** as a small
underlined text link below the upload button. Read as a whole it offered the
customer a decision about *how to pay* — and both branches named the counter, so
the one thing the customer actually had to do (scan the QR on the table in front
of them) appeared nowhere at all.

The real structure is **one method, two proofs**:

```
scan the DuitNow QR on your table          ← the only way to pay
   └─ then tell the cashier, either way:
        📷 upload the payment screenshot   ← Bedrock verifies it
        🙋 show the payment at the counter ← cashier confirms on their screen
```

The two proofs are genuinely equal in effort and outcome, so they are now equal in
weight: identical shape (full-width control plus caption), **measured identical at
348×56**, differing only in fill vs outline to mark the default. A filled button
beside an underlined link had encoded a preference that does not exist.

### 2. The live bug: the cart footer charged the ministry for a free order

`frontend/js/app.js` `renderCartPanel()` appended
`🏪 Pay at the counter after ordering` **unconditionally**. `preorderMode` was
already in scope and already governed the totals two lines above
(`cartTotal.textContent = preorderMode ? 'FREE' : …`), so the footer was the one
part of that panel that did not know which mode it was in.

Effect: a ministry volunteer building an RM 0 `MINISTRY_PREORDER` cart read **FREE**
as the total and *"Pay at the counter after ordering"* directly underneath it — on
the screen where they commit to the order. Now gated: `🎉 Free — nothing to pay`
for a pre-order, `📱 Pay by scanning the DuitNow QR on your table` otherwise.

Generalised into the `invariants` skill: **copy that asserts a domain fact is
gated on the state that makes it true.** It is the same shape as the v1.75.0
failure-vs-empty-success bug — a render site describing a state it never checked.

### 3. The placeholder was worse than nothing

The commented-out in-app QR block carried a placeholder image reference, dummy
account numbers, and the sentence *"Re-enable once real payment details are in
place."* That is a standing instruction to build a feature that must never
exist — and `frontend/img/README.md` said the same thing again, in a second
location, to a different reader. Both are deleted. Recorded as an invariant: **a
pending or disabled feature is deleted, not commented out, and its placeholder
assets and READMEs go with it.**

`frontend/img/qr-payment.svg` was never referenced by live code and was **not in
the `sw.js` `SHELL` array**, so it was never even precached. Deleted.

## Verified accurate and deliberately kept — do NOT "correct" these later

Each of these looked wrong at some point during the pass and was checked in the
source rather than reasoned about. Recorded so the next agent does not spend the
same time, or worse, "fixes" something that is right.

1. **`Instant AI verification — cashier gets notified automatically` is kept
   VERBATIM and is fully accurate.** Both halves were checked:
   - *AI verification* — `backend/src/routes/receipt.ts` imports
     `BedrockRuntimeClient` / `InvokeModelCommand` (`:4`), calls Bedrock to extract
     amount, date and reference (`:76`), and rejects a mismatch.
   - *cashier notified automatically* — `frontend/js/pos.js:454` calls
     `playReceiptSound()` when the receipt count rises; `pos.js:1212` renders a
     pulsing `💰 Receipt: RM…` badge; and v1.75.0's receipt-first sort tier lifts
     the order to the top of Pending.

   **One caveat found while verifying, not a copy defect:** the sound is gated
   `receiptCount > prevReceiptCount && prevReceiptCount > 0`, so the **first**
   receipt of a session plays no sound. The badge and the sort still fire, so
   "notified automatically" holds — but the audio cue specifically has a
   first-receipt gap. Not fixed here; see follow-up (x).

   An earlier draft of this pass believed the claim was false. **That was wrong.**
   It is recorded as checked-and-holding precisely so it is not "corrected" later.

2. **The ministry pre-order branch in `track.js` still reads
   `Nothing to pay. Come to the counter at your collection time and give your
   name.`** Correct as it stands: a pre-order is genuinely RM 0 /
   `MINISTRY_PREORDER`, so there is no QR to scan and no payment to prove. Verified
   on a rendered pre-order — no QR mention, no cash mention, no upload button.
   "Come to the counter" here is **collection**, not payment.

3. **`Collect your order at the counter` on READY, and the "until the counter
   starts it" strings.** Collection and order state. Not payment. Unchanged.

## Changes

All frontend. **No `backend/src/` file changed**, so no `npm run deploy:backend`
is required for this release.

- **`frontend/js/track.js`** — payment section restructured into one method plus
  two peer proofs; the `hasReceipt` branch split out of the template string; the
  error-path fallback button relabelled to match the new primary wording
  (`🙋 Show Payment at the Counter`, previously `🙋 Show to Cashier Instead`); the
  15-line commented-out in-app QR block deleted; and the comment above the
  pre-order branch extended to say why that branch has **no** QR instructions.
  Once a receipt is on file the payment instructions are no longer repeated —
  `receipt.ts` only writes `receiptUrl` after Bedrock matches the amount, so that
  state means paid, and repeating "how to pay" under a confirmation would
  contradict it.
- **`frontend/js/app.js`** — the cart-footer fix above, gated on `preorderMode`.
  A comment records that the footer used to be unconditional and must stay gated.
- **`frontend/js/pos.js`** — walk-up tag `🚶 walk-up · cash at counter` →
  `🚶 walk-up`. A comment records why, so the cash claim is not reinstated.
- **`frontend/css/style.css`** — six dead or superseded rules removed
  (`.qr-container`, `.qr-image`, `.qr-amount`, `.qr-hint`, `.receipt-upload-area`
  and its `p`); new `.pay-method`, `.pay-method-note`, `.pay-proof-lede`,
  `.pay-proof`, `.pay-proof-note`, `.pay-upload-btn`, `.pay-counter-btn`. The two
  proof controls share one box rule so they cannot drift apart, declared **after**
  `.upload-btn` on purpose — same specificity, so source order is what makes the
  shared box win over the inline-block default.
- **`frontend/img/qr-payment.svg`** — **deleted.** Never rendered; never in `SHELL`.
- **`frontend/img/README.md`** — rewritten to describe the `menu/` photos it
  actually governs (`<slug>.png`, missing files hidden by the `onerror` handler),
  plus an explicit line that the payment QR is physical and the app renders none.

No new `frontend/js` or `frontend/css` file, so the `sw.js` `SHELL` array is
unchanged. The only deletion is an asset that was never in it —
`npm run version:check` passes, which also confirms that.

## Version

**v1.75.0 → v1.75.1 (patch)**, via a single `npm run version:bump -- patch`. All
six markers and the top `changelog.json` entry written together; none hand-edited.

**Why patch and not minor.** The project's rule is *minor for features, patch for
fixes* (`.kiro/steering/release-checklist.md`), and this release adds nothing: it
corrects wrong copy, fixes one state-gating bug and deletes dead code. The
counter-argument was considered and rejected — this does materially change what
the app tells customers about paying, but there is no convention in this repo that
promotes customer-visible copy to minor, and the precedent runs the other way
(v1.70.1 and v1.70.2 were both patches for user-visible copy and visual tuning).

`frontend/changelog.json` v1.75.1 carries three volunteer-facing lines: that
paying is now spelled out as scanning the QR on the table; that there are two
equally easy ways to tell the cashier; and that a free ministry pre-order no
longer asks for payment. **Deliberately silent about the removed placeholder QR** —
a volunteer cannot act on it and it never rendered, so mentioning it would only
suggest that an in-app QR once existed.

## Verification

Every claim below was re-run in this pass against the tree as committed, not
carried over from the brief.

```
npm run version:check
  → ✓ All 6 version markers agree (v1.75.1), SHELL complete.

node --check frontend/js/{track,app,pos}.js
  → clean, all three

cd backend && npx tsc --noEmit
  → exit 0, no output

cd backend && npm test
  → 20 suites passed; 421 passed, 18 SKIPPED, 439 total
```

The **18 skips are `integration.test.ts` self-skipping** without credentials and
without `RUN_LIVE_WRITE_TESTS=1`. That is the correct behaviour, and it is reported
as a **skip, not a pass**. The backend was run despite no `backend/src/` file
changing, to prove that claim rather than assume it.

Also verified by reading the source, not by inference:

- `preorderMode` is module-scoped in `app.js` (`:61`) and therefore in scope at the
  cart-footer render site — the gate is real, not a lucky closure.
- Zero remaining references anywhere in `frontend/` to `qr-container`, `qr-image`,
  `qr-amount`, `qr-hint`, `receipt-upload-area` or `qr-payment`. The CSS removal
  and the asset deletion leave nothing dangling.
- The `Instant AI verification` claim, at the four sites listed above.
- `tmp/` and `.impeccable/` are gitignored (`.gitignore:33`, `:39`) and untracked;
  `tmp/customer-mock.html` is **not** staged.
- Diff read end to end for credentials: no key, PIN or token in it.

**Not run, deliberately:** `backend/tests/integration.test.ts` with credentials or
`RUN_LIVE_WRITE_TESTS=1`, and every Playwright journey. They write to the live
production café.

**Therefore unverified by execution — and this is the whole exposure of this
release:** the new payment section has **not** been seen rendered in a real
browser in this pass. Every layout claim in it (including the 348×56 measurement,
which was taken during implementation, not re-measured here) rests on reading the
CSS. The frontend still has **no automated test of any kind** — follow-up (w).
This is a pure-copy-and-CSS change on the customer tracking page, so the blast
radius of a visual mistake is a slightly wrong-looking payment card rather than a
wrong number; but *look at the tracking page* after the frontend publishes.

## Follow-ups

### Closed by this release

Nothing. **Checked rather than assumed:** no lettered follow-up (a)–(w) references
the `.qr-*` CSS rules, `frontend/img/qr-payment.svg`, or the payment section — the
whole of `docs/` was grepped for `qr`, `duitnow`, `pay at the counter` and
`payment section`. So there is nothing to mark resolved.

### New

x. **`frontend/js/training-config.json:110` still teaches the wrong payment
   fact** — the cashier tutorial step for walk-up orders ends *"The customer pays
   at the counter."* This is a **sixth** location, found by grepping for the wrong
   fact after fixing the five above, and it is user-visible copy: it is what the
   POS tutorial teaches a new volunteer. **Deliberately not fixed in this
   release** — it is outside the change set the user enumerated and reviewed, and
   slipping an unreviewed copy edit into a release they are about to ship is worse
   than carrying it one release. It is a one-string fix and should be the first
   thing in the next one. Suggested replacement: *"The customer scans the DuitNow
   QR on their table and shows you the payment."*

y. **The receipt sound never fires for the first receipt of a session.**
   `frontend/js/pos.js:454` gates on
   `receiptCount > prevReceiptCount && prevReceiptCount > 0`, so the transition
   from **0 → 1** receipts is silent. The `prevReceiptCount > 0` half exists to
   stop the sound firing on the first poll after login (when the count goes from
   its initial `0` to whatever is already on the board), which is correct — but it
   also swallows the genuine first receipt of the morning, which is the one a
   cashier is least primed to expect. The badge and the receipt-first sort still
   fire, so `Instant AI verification — cashier gets notified automatically` remains
   true. Fix is to distinguish "first poll" from "count was genuinely zero", e.g. a
   `prevReceiptCount === null` sentinel rather than `0`. Found while verifying the
   copy claim; not fixed, because it is behaviour and this release is copy.

z. **`docs/sop-reference.md:69` contains a shared device credential in plain
   text** — an `**iPad password:** <6 digits>` line; the value is deliberately not
   repeated here, so this note does not become a second copy of it.
   **Pre-existing and NOT in this release's
   diff**; recorded only because it was read during the documentation sweep. It is
   the café's counter iPad in a repository that has been described elsewhere in
   this file as public. Worth a decision from the user; deliberately untouched
   here, since rotating or redacting a real device credential is their call and
   not a release-manager edit.

### Still open, unchanged

- **(n), (o), (p)** — `prep.html` missing from the `SHELL` array; the dead
  `generateCollectionSlots()`; the `#wkNotes` placeholder wording.
- **(a) downgraded, (c), (d), (f), (g), (h), (i), (k), (l), (m), (q), (r), (s),
  (u), (v), (w)** — all unchanged by this release.

## Documentation and skills updated in this pass

- `frontend/changelog.json` — v1.75.1, three volunteer-facing lines (written by
  `bump-version.mjs`, not by hand).
- `docs/feature-history.md` — new **2026-08-18 Sprint — v1.75.1** entry. Also
  **Important Context corrected**: its payment line read only *"Payment: Maybank QR
  (DuitNow), manually verified by cashier"*, which is true but omits the two facts
  whose absence caused this bug — QR-only, and the QR is physical.
- `docs/update-20260817.md` — this section; follow-ups x, y and z added.
- **`.kiro/steering/project.md`** — **Operating context** payment line rewritten
  to state QR-only, physical/tabletop, no in-app QR, and the two proofs. This is
  the file an agent is told to read first, and it was the most upstream place the
  fact could have been learned correctly. The layout block is **unchanged and
  verified**: no new `backend/src/routes/*.ts`, `backend/src/lib/*.ts` or
  `frontend/js/*.js`; all changed modules already appear in it.
- **`.kiro/skills/invariants/SKILL.md`** — three additions to **Frontend**, each
  with the production bug that motivated it:
  1. *Copy that asserts a domain fact is gated on the state that makes it true* —
     the pre-order cart-footer bug.
  2. *The house payment fact* — QR-only, physical, no "pay at the counter" or
     "cash" on any surface; and the note that collection copy is not payment copy,
     so it is not over-corrected.
  3. *A pending or disabled feature is deleted, not commented out — placeholder
     assets and READMEs included* — the in-app QR block, `img/README.md` and
     `qr-payment.svg`.

  Frontmatter `description` extended so the skill is found for customer-facing
  payment copy, and `.claude/skills/invariants/SKILL.md`'s stub description
  re-synced to match it byte for byte. `.kiro` remains authoritative.
- `docs/requirements.md` §3.3 — **it was actively wrong.** It said *"After
  submitting, customer sees total amount + Maybank QR code to scan"*, i.e. it
  specified the in-app QR that must never exist, and *"Customer shows payment proof
  to cashier at counter"* as the only proof. Rewritten: QR-only, physical, and one
  method with two equally weighted proofs.
- `docs/user-journey.md` — the sequence diagram had `App-->>C: Show total + Maybank
  QR code` / `Note over C: Customer scans QR & pays`, which reads as the app
  displaying the QR. Corrected, with the physical-QR fact as a note, and the single
  "shows payment proof" step replaced by the two proofs. Its opening
  `Scan QR code at table` step was also clarified to **app-link QR** — with a
  payment QR now on the same tabletop, an unqualified "QR code at table" is
  genuinely ambiguous.

### Checked and deliberately NOT changed

Each row of the release brief's documentation table was worked; these did not fire,
and the reason is recorded rather than left blank.

- `.kiro/skills/api-reference/SKILL.md` — **no endpoint, method, path or auth
  requirement changed.** No `backend/src/` file was touched at all. `POST
  /api/receipt` and the Bedrock verification it performs are already documented and
  already accurate; the copy that describes them to the customer changed, not the
  contract.
- `.kiro/skills/db-schemas/SKILL.md` — no table, GSI, TTL or record attribute
  changed. `receiptUrl` / `receiptAmount` / `receiptDate` / `receiptRef` are
  documented already and untouched.
- `.kiro/skills/order-lifecycle/SKILL.md` — no status transition, guard or
  food-counter effect touched. Nothing about approve, release or expiry moved.
- `.kiro/skills/pricing-rules/SKILL.md` — **no pricing or discount rule touched.**
  Payment *method* is not pricing: no money, total, class, offset or reprice path
  is affected, and `MINISTRY_PREORDER` still zeroes the order exactly as before.
  The cart footer now *describes* the RM 0 correctly; it does not compute it.
- `.kiro/skills/test-suites/SKILL.md` — no suite added, no suite changed, no new
  test-data marker, and nothing new writes to production. The category-1/2 split
  and the `ZZTEST_` contract are unaffected.
- `.kiro/skills/release-flow/SKILL.md` and `.kiro/steering/release-checklist.md` —
  **the release and deploy procedure itself did not change.** Both were read in
  full at the start of this pass; the six markers, the `SHELL` rule, the pre-deploy
  list and the two asymmetric deploy paths are all still accurate as written.
- **No new skill created**, and therefore no new `.claude/skills/` stub and no new
  `CLAUDE.md` row. Payment is not a new subsystem — it is a house fact plus three
  render sites, and both belong in `project.md` and `invariants`, which now carry
  them. A "payments" skill would be one paragraph pointing at two files.
- `docs/architecture.md` — **read and not contradicted.** §10.1 *Payment Receipt
  Verification* describes the Bedrock path (upload → extract amount/date/reference
  → validate → auto-reject on mismatch) and is accurate; it never claimed the app
  renders a QR. No component, data flow or integration changed.
- `docs/sop-reference.md` — **accurate as written**, and left alone. Its *Payment &
  Pricing* section already says *"Payment method: QR payment (Maybank DuitNow)"*
  with no cash or card claim; it is the café's own operational SOP and describes
  reconciliation, not app copy. (Its plaintext iPad password is follow-up (z).)
- `docs/pos-ux-audit-20260818.md` — left as the dated point-in-time record it is,
  for the same reason given in the v1.75.0 section.
- `docs/problem-statement.md` — mentions manual DuitNow reconciliation, which is
  still true. Not contradicted.

## Still for the user to run — v1.75.1

**Nothing is pushed and nothing is deployed.** One commit on `master` on top of
`5a03f97` (v1.75.0, which **is** already pushed and deployed).

```bash
npm run deploy:frontend    # git push; Actions publishes Pages
```

- **`npm run deploy:backend` is NOT needed.** No `backend/src/` and no `infra/`
  file changed. Running it would be a no-op redeploy, not a risk, but there is
  nothing in it for this release.
- **The Pages workflow will fire.** It only triggers on pushes to `master` touching
  `frontend/**`, and this commit touches six files under `frontend/` — so the
  usual trap (a frontend fix bundled into a backend-only commit, silently never
  shipping) does not apply here.
- **`deploy:frontend` runs `version:check` as `predeploy:frontend`.** Clean as
  committed, at v1.75.1.
- **No API response shape changed**, so old cached service-worker shells still on
  `rlc-cafe-v1.75.0` remain compatible while clients update. A cashier mid-service
  is unaffected.
- **Look at the customer tracking page after it publishes** — this is the one
  thing this pass could not verify. Use **Check for update** in the footer, or pull
  to refresh, to be sure you are on the `rlc-cafe-v1.75.1` shell first, then open a
  PENDING order and confirm: the tabletop-QR instruction is the headline, the two
  proof controls look like peers rather than a button and a link, and a **free
  ministry pre-order** shows no payment instruction at all.
- **Then consider follow-up (x)** — the POS tutorial still tells a new cashier that
  the customer pays at the counter. One string, in
  `frontend/js/training-config.json`.

---

# v1.76.0 — customer-screen pass (work done 2026-08-19)

## Context

An `/impeccable` critique of the two customer-facing pages — `index.html` (menu +
cart) and `track.html` (order tracking) — scored the pair **23/40**. The user
previewed the proposed build and approved it. This pass is that build, plus a
security sweep that turned out to be the larger half of the work.

Five files, **frontend only**: `frontend/js/{app,track,variants}.js`,
`frontend/index.html`, `frontend/css/style.css`. **No `backend/src/` file
changed** — confirmed from `git status`, not assumed.

## The finding that matters most: four escapers that were incomplete

The brief was "three known raw sites" (`customerProfile.name`, the name input
`value`, the search input `value`). The sweep found many more — but the raw sites
were not the interesting part. **Four inline escapers were partial**, and every
one of them was being *called* at its render site:

| Was, in `HEAD:frontend/js/app.js` | Covered | Missing |
|---|---|---|
| `esc` at `:226` | `< > &` | `"` `'` |
| `esc` at `:251` | `< > &` | `"` `'` |
| `escAttr` at `:275` | `"` only | `& < > '` |
| `escText` at `:276` | `< > &` | `"` `'` |

Verified directly, not taken on report:

```
$ git show HEAD:frontend/js/app.js | grep -nE "const esc|replace\(/&/"
226:  const esc = s => String(s || '').replace(/[<>&]/g, ch => ({'<':'&lt;','>':'&gt;','&':'&amp;'}[ch]));
251:  const esc = s => String(s || '').replace(/[<>&]/g, ch => ({'<':'&lt;','>':'&gt;','&':'&amp;'}[ch]));
275:  const escAttr = s => String(s || '').replace(/"/g, '&quot;');
276:  const escText = s => String(s || '').replace(/[<>&]/g, ch => ({'<':'&lt;','>':'&gt;','&':'&amp;'}[ch]));
```

**Why this is worse than a missing escape.** A missing escape leaves an absence a
reviewer can notice. A partial escaper leaves a *call* — `esc(item.name)` — at the
render site, which reads as done. The only way to see the hole is to open the
helper's body. Note also that the top-level `escHtml` in both `app.js` and
`track.js` was **already complete** (all five characters, at `:19` and `:9`), so
"this page has a correct escaper" was simultaneously true and misleading; the
partial copies were function-local and shadowed nothing.

`escAttr` at `:275` is the sharpest of the four: it guarded a **quoted
attribute**, which is exactly the context where an unescaped `'` or `&` breaks
out, and it escaped neither.

All four are deleted rather than completed — a completed copy is just the next
drift. Both pages now route every non-literal string through the one complete
`escHtml`. Confirmed after the change: `app.js` contains exactly one escaper
definition.

### The load-bearing field

`customerProfile.name` comes from `GET /api/customers/{phone}`. Verified the
endpoint exists rather than trusting the report:
`backend/src/routes/customers.ts:188` dispatches `method === 'GET' && phone` to
`lookupCustomer`, with `extractPhone` at `:170` doing the path parse itself (the
usual dispatcher rule — API Gateway proxy integration does not populate
`pathParameters`). So the name is **stored and cross-user**: one person's name
renders inside another person's page. That is the difference between a
self-inflicted curiosity and something a stranger can aim.

### Sites closed

`item.description`; item names in the card **and** the featured hero, including
their `aria-label`s; slugs interpolated into `src`; `item.id` into `data-id`; the
pre-order banner message and name; the staff-link label; the collection-time
options; the verse text and reference; `flaggedItems`; `order.reason` (cashier
free text on the cancel dialog); `orderId` and `date` in the order history; plus a
missing `encodeURIComponent`. `variants.js` alone had **eleven** raw sites.

The counterpart rule was respected, not over-corrected: `showError` / `showSuccess`
on `track.js` and the new `refreshChosenSummary` in `variants.js` all assign
`textContent` and are therefore **not** escaped. A comment says so at each, so the
next sweep does not "fix" them into rendering literal `&quot;`.

## The `variants.js` escaper — a deliberate 5th exception

`variants.js` now carries its own module-private `esc()`. This is duplication, it
is intentional, and it is recorded in `.kiro/skills/invariants/SKILL.md` so nobody
consolidates it away:

`variants.js` is loaded by **`index.html`, `track.html` and `pos.html`**. Those
three bundles do not agree on a name for their escaper — they define `escHtml`,
`escHtml` and `escapeHtmlPos` respectively. There is therefore **no sibling global
that exists on all three pages**. Reaching for `escHtml` emits raw HTML on
`pos.html`; reaching for `escapeHtmlPos` emits raw HTML on the two customer pages.
Either choice is a live bug on one of the three, and it would be an *invisible*
one, because each page would look fine when tested alone.

The alternative — a shared cross-page util module — costs a new file in the `sw.js`
`SHELL` array plus a `<script>` tag on every page, which is the same reason the
existing per-bundle split was accepted. Variant markup is built in exactly one
function, so its escaping still lives in exactly one place.

This makes **five** accepted entries on the do-not-duplicate exception list:
`escapeHtmlPos` (`pos.js`), `escHtml` (`app.js`), `escHtml` (`track.js`), the
inline `esc()` in `prep.html`, `escapeHtml`/`escapeAttr` in `admin.js` for the
whole admin bundle — and now `esc()` in `variants.js`.

## The cart footer — measured before and after

`index.html`'s cart overlay is now three bands, in order and not nested deeper: a
fixed head, **one** scrolling region, and a pinned foot. Previously the total and
the Place Order button sat at the end of a single long scroll.

With five drinks in the cart, distance of the primary action **below the bottom
edge**:

| Viewport | Before | After |
|---|---|---|
| 390 | 322px below | 21px clear |
| 932 | 249px below | 21px clear |
| 1024 | 174px below | 21px clear |

The 1024 row is the one worth keeping: this was not a small-phone problem. The
primary action of the whole page was off-screen **on the counter tablet too**, and
the total — the number a customer checks before committing — with it.

## The 493px shell, and why the collapse is not the answer

The critique's premise about menu density was that the card was ~372px and 14
items came to ~5,200px. **Both numbers were wrong.** Measured:

| | Assumed | Measured |
|---|---|---|
| Card height | 372px | **282px** |
| 14 items, total scroll | ~5,200px | **4,575px** |

The `grid` layout was measured as a candidate fix and is **worse per card** —
520px against 282px — and, decisively, **zero cards are fully above the fold in
either layout**. So `list` stays the default; `grid` was not made the default and
should not be, on this evidence.

The collapsible picker was applied anyway and does help a little: median card
282 → 241px, total scroll 4,575 → 4,075px. But it is a **minor lever**, and this
is the measurement to keep:

> **The shell above the first card is 493px — 58% of an 844px viewport.**

No arithmetic on card height gets a **second drink** fully above the fold while
that stands. Shaving 41px per card cannot compete with 493px of chrome. Anyone who
returns to menu density and starts by making cards smaller will spend the effort
and not move the number.

**The real next lever is the name wall** at the top of the page. It is the bulk of
that 493px, and it is asking for something the app asks for again anyway:
`promptName()` already collects the name at checkout. A congregant who wants to see
the menu is made to identify themselves first, for information the checkout will
request regardless. Removing or deferring it is the change that would actually get
a second card above the fold. Recorded as follow-up (y).

## Changes

- `frontend/js/app.js` — four partial inline escapers deleted, all sites routed
  through the single complete `escHtml`; cart lines now each render a price (staff
  mode shows struck gross beside RM 5.00 on drinks, pre-order mode shows FREE per
  line and makes no payment claim); past orders fetched once per load rather than
  once per 7s poll; 404 split from offline; sold-out card token swap.
- `frontend/js/track.js` — the unprompted `Notification.requestPermission()` on
  load **deleted**; the dialog is now raised from the "Yes, notify me" tap;
  `vapidRes.ok` and `publicKey` checked, `push/subscribe` response checked, and
  all three failure outcomes (denied / dismissed / setup failed) now say
  something; `order.reason`, `flaggedItems`, verse text and reference escaped;
  404 → `This order has closed` with links out and **polling stopped**, 500 → its
  own screen that keeps polling; two-line wait on `Preparing`.
- `frontend/js/variants.js` — module-private complete `esc()` with the cross-page
  reasoning in a comment; eleven raw sites closed; group **names** printed, with
  `role="group"`, `aria-label` and `aria-pressed` on every option; optional groups
  marked `optional`; opt-in `collapsible` picker with a `Change`/`Done` disclosure
  and a live one-line chosen-options summary.
- `frontend/index.html` — cart overlay restructured into head / scroll / foot,
  with a comment recording the three measured before-numbers so the structure is
  not casually flattened again; `cartClose` given an `aria-label` and a class
  instead of inline styles.
- `frontend/css/style.css` — the three cart bands; `.track-wait`; the
  `.load-failure` screens; collapsible-picker and group-label styles; sold-out
  card contrast.

### The notification bug, stated precisely

`track.js` called `Notification.requestPermission()` at module load — with **no
user gesture**, and, worse, **no `subscribe()` after it**. Consequences, in order:

1. The browser dialog appeared before the 🔔 banner explaining it had rendered.
2. The one permission grant a customer will ever be asked for was **spent** there.
3. Nothing subscribed, so even a granted permission produced no subscription.
4. Tapping "Yes, notify me" afterwards called `requestPermission()` again, got the
   already-decided answer, hit `if (permission !== 'granted') return;` and
   **returned in silence** — no message, no error, nothing on screen.

Note this is adjacent to, but not the same as, the v1.73.0 push fix (wipeable
VAPID env vars). That one made delivery fail server-side; this one made
subscription fail client-side. The `vapidRes.ok` check added here is what would
have surfaced v1.73.0's failure instead of swallowing it: unchecked, the response
fell through to `urlBase64ToUint8Array(undefined)`, which throws inside `atob` and
lands in a `console.error` nobody reads.

### `aria-pressed` had regressed

Worth recording because it inverts the usual assumption. The legacy `.variants`
path always set `aria-pressed`. The newer `.variantGroups` path — the one that
replaced it — did not, leaving it `null`. **The newer code was the less accessible
of the two.** Newer is not a proxy for better; the replacement was never checked
against what it replaced.

## Verification

Re-run against this tree, not trusted from the incoming report.

| Check | Result |
|---|---|
| `node --check` on `app.js`, `track.js`, `variants.js` | clean, all three |
| `npx tsc --noEmit` (backend) | exit 0 |
| `npm test` (backend, `TZ=UTC jest`) | **20 suites passed, 421 passed, 18 skipped**, exit 0 |
| `npm run version:check` | clean at v1.76.0, all six markers agree, SHELL complete |
| `git status` | five frontend files only; no `backend/src/`, no `infra/` |

**The 18 are skips, not passes.** They are `tests/integration.test.ts`
self-skipping for want of credentials — `describeAuthed = hasAdminCreds ? describe
: describe.skip` at `:24`, with the same pattern at `:44`, `:115`, `:116`, `:210`.
No credentials were supplied and `RUN_LIVE_WRITE_TESTS` was **not** set, so
nothing in this pass wrote to production. No Playwright journey was run.

**No new `frontend/js` or `frontend/css` file** was added — all five changed files
already existed — so the `sw.js` `SHELL` array needed no addition.
`version:check` confirms `SHELL complete` independently.

**Not verified in a browser.** Every measurement in this section came in with the
critique and is recorded as reported: the 322/249/174px and 21px cart figures, the
282px / 4,575px / 520px card numbers, the 493px shell, the contrast ratios
(2.25:1 → 8.43:1, 2.96 → 5.62:1, 2.49 → 4.64:1) and the 1-request-in-16s polling
result. The code changes that produce them were read and confirmed present; the
pixel values themselves were not independently re-measured in this pass. Treat
them as design intent with a measured provenance, not as this pass's evidence.

## Follow-ups — new in this pass

- **(y) The name wall is the next menu-density lever.** Not the card height. The
  shell above the first card is 493px, 58% of an 844px viewport, and the name
  prompt is the bulk of it. `promptName()` already asks at checkout, so the
  up-front ask is collecting something the app collects again anyway. Deferring
  it is the only change on the table that gets a second drink above the fold.
  Everything else is 41px at a time against a 493px problem.
- **(aa) Sub-44px tap targets on the customer pages.** `.cart-item-actions button`
  is `36px × 36px` (`style.css:806-808`) and that selector covers the `−`, the `+`
  **and** the remove `✕` on every cart line. The project declares its own floor at
  `style.css:63` — `--tap:44px; /* the floor for every touch target */` — so this
  is a violation of a stated house rule, not a matter of taste. The new
  `.cart-panel-close` was built to `var(--tap)` and is fine; a second 36px pair at
  `:877` and a `min-width/min-height:36px` at `:2810` should be checked in the same
  pass.
- **(ab) `[−]` is a silent no-op after switching a variant.** `app.js:595-601`:
  the `dec` branch rebuilds `variantKey` from the picker's **current** selection,
  then removes the last cart line matching `id + variantKey`. Add "Latte Hot",
  switch the picker to Iced, tap `−`: `variantKey` is now `Iced`, the loop matches
  no line, and `saveCart()` / `renderMenu()` both run having changed nothing. No
  message, no visible effect — the customer taps a control and the app does not
  respond. Either match the last line by `id` alone when the keyed lookup misses,
  or disable `−` when it would find nothing. Related to (aa): the same 36px button.
- **(ac) The featured drink renders twice.** `app.js:389` emits the
  `featured-drink-hero`, and the item is **not** removed from `filteredGrouped`
  before `:414` renders the ordinary cards, so it appears again below. Worse, the
  hero's `+` carries `data-action="inc" data-id="${featItem.id}"` — the *same*
  `data-id` as the duplicate card — and the shared handler at `:553` resolves the
  selection via `getSelectedVariants(id)`, which reads the picker **in the card
  below the hero**. So the hero's `+` silently adds whatever that other widget
  happens to be showing. Either exclude the featured item from the grid, or give
  the hero its own picker.
- **(ad) The closed-café screen hardcodes the opening times and the day.**
  `app.js:911-912` renders `See you next Sunday! ☕` above
  `⏰ Opens 10:15 AM & 12:45 PM`. Both are literals with no state behind them, so
  at **09:50 on a Sunday** — 25 minutes before the café opens, congregant already
  on the premises — the app tells them to come back **next week**. The times are
  also a second copy of a fact that belongs to café settings, and
  `pos-checklist.js:23` carries the same `See you next Sunday!` string. This is the
  copy-gating invariant (copy asserting a domain fact must be gated on the state
  that makes it true) failing on the customer's first screen. Highest
  customer-impact item on this list.

## Documentation and skills updated in this pass

- `.kiro/skills/invariants/SKILL.md` — `variants.js` added as the **5th** accepted
  entry on the do-not-duplicate exception list, with the three-page /
  three-different-names reasoning spelled out and an explicit "do not consolidate".
  Separately, the escaping section's claim that `escHtml` in `app.js` / `track.js`
  "already do" cover all five characters was **corrected**: true of those two
  helpers, but it read as "the customer pages are done", which is what this pass
  disproved. A new assertion now says a page having a complete escaper is not
  evidence the page is escaped, that a partial escaper is more dangerous than a
  missing one, and that escaper **bodies** must be audited rather than call sites.
  Frontmatter `description` updated to carry both, so the skill is still found by
  an agent searching for escaping guidance.
- `.claude/skills/invariants/SKILL.md` — pointer stub; `description` frontmatter
  synced to match. Body unchanged, still points at the `.kiro` copy.
- `docs/feature-history.md` — v1.76.0 entry; `Current Status` date moved to
  2026-08-19.
- `frontend/changelog.json` — seven volunteer-facing entries, written via
  `version:bump`. The escaping work is **deliberately not** described there: it is
  not a volunteer-actionable change, and a changelog is a poor place to publish the
  shape of a vulnerability. It is documented here and in the `invariants` skill,
  which is where someone touching this code will look.

### Table rows deliberately skipped

- `api-reference` — no endpoint added, changed, or re-authorised. `GET
  /api/customers/{phone}` was *read* to confirm the escaping rationale, not
  modified.
- `db-schemas`, `order-lifecycle`, `pricing-rules` — no table, GSI, TTL,
  attribute, status transition, guard, food-counter effect, or pricing rule
  touched. The cart's per-line price display is a render of existing
  `price`/`grossPrice` fields; `CafePricing` still decides the numbers.
- `.kiro/steering/project.md` — no new `backend/src/routes/*.ts`,
  `backend/src/lib/*.ts` or `frontend/js/*.js` module. All five changed files are
  already in the layout block.
- `test-suites` — no suite added, no suite newly writes to production, no new
  test-data marker. Nothing was run against the live API in this pass.
- `release-flow` / `release-checklist.md` — the procedure itself did not change.
- `docs/architecture.md`, `docs/requirements.md` — checked; nothing in this pass
  contradicts either. It is presentation, escaping and one deleted premature API
  call, all within the existing architecture.

## Still for the user to run — v1.76.0

**Nothing is pushed and nothing is deployed.** One commit on `master` on top of
`256bd29`, which is v1.75.1 — already pushed and deployed.

```bash
npm run deploy:frontend    # git push; Actions publishes Pages
```

- **`npm run deploy:backend` is NOT needed.** No `backend/src/` and no `infra/`
  file changed. It would be a harmless no-op redeploy with nothing in it.
- **The Pages workflow will fire.** It triggers only on pushes to `master` touching
  `frontend/**`, and this commit touches ten files under `frontend/` (the five
  changed sources plus the five version markers) — so the asymmetry trap (a frontend fix bundled into a backend-only commit, silently never
  shipping) does not apply.
- **`deploy:frontend` runs `version:check` as `predeploy:frontend`** — clean as
  committed, at v1.76.0.
- **No API request or response shape changed**, so clients still holding the
  `rlc-cafe-v1.75.1` shell stay compatible while they update. A cashier mid-service
  is unaffected. The one behavioural change visible across the boundary is that
  `track.js` stops polling a dead order id, which reduces load rather than adding
  any.
- **Check in a browser after it publishes** — this pass ran no browser, so the
  pixel and contrast figures above are unconfirmed here. Use **Check for update**
  in the footer to be sure you are on the `rlc-cafe-v1.76.0` shell, then:
  1. Put five drinks in the cart on a phone. The total and **Place Order** should
     be visible without scrolling, with only the rows moving.
  2. Every cart line should show a price.
  3. Open a drink with options — the rows should be labelled (Temperature, Milk),
     with a `Change` disclosure on the menu.
  4. Open `track.html?orderId=` with a **made-up id**. Expect
     `This order has closed` with links out, not a spinner.
  5. On a real PENDING order, tap **Yes, notify me** and confirm the browser
     dialog appears **at that moment** — and that a refusal now produces a message.
- **Then consider (ad)** — the closed-café screen telling a congregant "See you
  next Sunday!" at 09:50 on a Sunday is the highest customer-impact item left, and
  it is two strings in `app.js:911-912`.

---

# v1.77.0 — the palette replacement (work done 2026-08-19)

## Context

The user's own framing: the visuals "were all done poorly earlier as we focus
more on functionality earlier." They granted full authority over the repaint,
were shown three directions, chose **"deliberate warm"**, then reviewed and
approved the mock at `tmp/palette-mock.html` (gitignored — a reference, never
committed).

**This release is deliberately isolated.** The user agreed the palette ships
alone, with nothing else in it, so that a visual regression cannot be confused
with a behavioural one and `git revert ` stays a real option. Two items were
folded in only because both were pre-agreed as *belonging* to the repaint (the
transform-based sidebar push and the halo offsets, below). Nothing else was.
Notably, the invalid `manifest.json` was **not** repaired — see follow-up (ae).

25 files changed, all under `frontend/`. No `backend/` or `infra/` file touched
(verified: `git status --porcelain | grep -E '(backend|infra)/'` → empty).

## The direction, and why these values

- **True paper `#FFFFFF`** instead of cream. The old cream world was the single
  biggest cause of the low contrast ratios below: every ratio is computed against
  the surface, and the surface was already spending contrast.
- **Espresso ink `#241A14` — 17.03:1.** Body text now has headroom rather than
  scraping the floor.
- **Warm secondary `#6B5A4C`**, espresso bands `#3A2A1F`, and a raised step of
  `#EFEAE1`. No grey anywhere in the warm world.
- **Burnt-orange brand `#B4531C`, split in two.** The fill measures **4.18:1 on
  the raised surface `--quiet`** — under the body floor — so text takes
  `--brand-ink #8F3F11` (7.27:1 on a card) and the fill stays a fill. This split
  is the reason two tokens exist where a designer would expect one; it is not
  redundancy.

**87 contrast pairs were measured in-browser: 76 PASS, 10 WCAG-exempt or
documented bans, 1 diagnostic FAIL.** That single FAIL is `--brand` on `--quiet`,
and it is kept in the table on purpose: it is the evidence for the brand split,
and no site in the app uses that pair.

## The system rule: `--brand` never appears on the POS board

**This will look arbitrary to anyone who finds it later and wants to put orange
buttons on the board. It is not. Record and keep it.**

Burnt orange sits *inside* the warm band that the app's order-status semantics
already own:

| Pair | Normal | Deutan | Verdict |
|---|---|---|---|
| `--brand #B4531C` vs `--warning #A87700` | ΔE 9.2 | **ΔE 6.6** | **FAIL** |
| `--brand #B4531C` vs `--danger #A81E13` | **ΔE 9.9** | — | **FAIL** |

> **One figure to be aware of.** The brief for this release quoted the
> brand-vs-warning deutan separation as **ΔE 4.7**; the shipped
> `frontend/css/style.css:22`, written at measuring time, records **ΔE 6.6**. The
> CSS value is used here and in the `invariants` skill because it is the artifact
> that carries the measurement next to the values it was taken from. Both FAIL, so
> the rule is unaffected either way — but if the figure is ever cited as evidence,
> re-measure rather than trusting either number.

No recognisable burnt orange clears both — the colour that would is no longer
burnt orange. So an orange primary on the board is a button a deutan cashier may
read as a warning or as a rejection, on the one screen where a misread costs a
wrong order.

The rule, therefore: **`--brand` appears on the POS board in exactly one place,
the header wordmark.** Board buttons are espresso; the confirm action is
ready-green. Admin is unaffected and orange is wanted there, because admin has no
live order-status band to collide with.

Verified at ship time by a **rendered-DOM hue scan: 0 occurrences on the board,
11 on admin**, which is where the rule wants it. Re-verified statically for this
release: no rule body under a `.pos-` or `.board` selector contains
`var(--brand)`. `--brand-ink`, `--brand-soft` and `--brand-deep` are text and
tint tokens and are not covered by the ban.

## A live accessibility defect, fixed: ΔE 0.4 → 6.0

This is the most important functional content of a release that is nominally
cosmetic.

The **shipped** pre-order violet and preparing blue measured **ΔE 0.4 deutan** —
that is *the same colour* for a deutan cashier — and 12.4 normal-vision. **Both
FAIL.** Two states a cashier must act on differently were indistinguishable to a
colour-blind volunteer, and had been since they shipped. Verified independently
before changing anything.

| | Before | After |
|---|---|---|
| Pre-order ribbon vs its card | 5.70:1 | **13.72:1** (espresso; violet retired) |
| Later-service variant vs its sibling | 3.09:1 | **8.47:1** |
| Worst adjacent status separation, 6 states | **ΔE 0.4 deutan** | **ΔE 6.0 deutan** |

Every state also now carries a **word**, not just a hue. Colour is the redundant
channel, not the primary one. Recorded as an invariant.

## The receipt indigo is deliberately unchanged — do not "harmonise" it

`#4338CA` stays. "Money has arrived" is genuinely not a warm-band event, and it
could not be made to pass on hue alone against the warm statuses. Its channel is
therefore **shape**:

- a **2px full-card outline**, and
- a **`RECEIPT SENT — check it`** band.

That secondary encoding is **mandatory, not decorative**. It is what carries the
meaning; the indigo is the decoration. If a later pass removes the outline or the
band "because the colour already says it", the state disappears for a deutan
cashier while looking fine to the person removing it.

## Dashboard chart trio re-keyed — and NOT for contrast

New trio: `#1F6FB2 · #0C8C63 · #93276A`. Validated **ALL CHECKS PASS** on the new
surface. Slot 2 unchanged.

The reason was **not** contrast. The old trio also passed, and always had, because
`admin.css` sets `--dash-surface:#fff` — so the marks were on white regardless of
the page's cream world. The actual reason: `--src-1 #B4691C` was **three hex
digits** from the new brand accent `#B4531C`, and two other charts on that same
dashboard use the accent family. A categorical series that is nearly the brand
colour stops reading as a category.

**Accepted cost, recorded so it is not rediscovered as a bug:** tritan separation
**9.4 → 4.5**. Mitigated by the legend, the in-segment counts and the table view —
three non-colour ways to read the same numbers.

## Three deviations from the approved mock

The mock is the reference document, so an unexplained difference reads as a
mistake and gets reverted. Each of these was a measurement, not a preference:

1. **`scrollbar-color`.** The mock specifies `var(--tan)`, which measures
   **2.01:1**. The shipped CSS *already carried a comment* that a near-invisible
   scrollbar thumb had been a real usability problem here. Shipped `--field-bd`
   (**3.18:1**) instead.
2. **`--tan` demoted to decorative-only.** The mock never measured it. It is
   **2.73:1 on white** — it may not carry text, a border, or a scrollbar thumb.
3. **Five white-labelled gradients flattened.** They were built
   `--primary` → `--primary-light`; white on the mock's light end is **3.26:1**.
   Flattened to solid `--brand` (**5.01:1**). `--brand-light #C97C40` survives as
   chart/fill-only and is commented as such at its definition.

## Five pre-existing defects found and fixed en route

All five predate this release. None was introduced by the repaint.

1. **Three tokens were never defined at all** — `--brown`, `--white`,
   `--cream-lighter`. Their `var(…, fallback)` was therefore *the live value*.
   This is how the old `#6B4226` brown stayed pinned into the walk-up screen,
   behind a token name for which no definition existed to grep for.
2. **261 `var(--tok,#hex)` fallbacks were a second, drifted palette.**
   `--cream-dark` alone was written with **six different values**
   (`#E5DACB #E8E0D8 #E7DFD5 #F5EDE4 #EEE #DDD`), so "the" colour depended on
   which rule you happened to land in. All 261 stripped — a **render no-op**,
   because every page loads `style.css` and every token is now defined.
3. **Every text input in the app failed WCAG 1.4.11 at 1.58:1** — and the fix
   needed *all* of them, not just the one the brief named: `.pos-input`, both note
   textareas, `.stock-result-input`, the admin form inputs, the number input and
   `.dash-date-select`. New `--field-bd` at **4.33:1**.
4. **Two placeholders composited below the floor via `opacity:.8`** — 3.62:1 and
   4.09:1. **The opacity was the bug**, not the colour. Dimming text is a
   contrast change, not a styling choice.
5. **Two further cool greys the earlier sweep missed** — `#F3F4F6` / `#374151` on
   the tracking status pill, and `#D1D5DB` on the POS switch track.

## Also folded in — both pre-agreed as belonging to the repaint

- **`.pos-main` and `.admin-main` converted from animating `margin-left` to a
  transform-based push.** These were the **last two layout-property animations**
  in the codebase, tracked as follow-up **(a)** since v1.75.0 — now **CLOSED**.
  `transform:translateX(220px)` / `translateX(240px)` on the sidebar-open sibling
  selector, each with a `prefers-reduced-motion:reduce` opt-out. Verified:
  `grep -rE 'transition:[^;}]*(margin|width|height|top|left|right|bottom|padding)' frontend/css/*.css`
  returns nothing.
- **Zero-offset coloured halos given an offset and a blur** — except
  `@keyframes training-cue-pulse`, which is a **functional tutorial spotlight**
  (it marks the button being demonstrated) and was left alone deliberately.
  Confirmed untouched by the diff.

## Changes

- `frontend/css/style.css` — the `:root` palette replaced wholesale; the measured
  ratios and the two FAIL pairs recorded as comments at the definition sites;
  colour fallbacks stripped; `--field-bd` added; status colours re-keyed;
  `.pos-main` transform push; halo offsets.
- `frontend/css/admin.css` — admin-only tokens; dashboard chart trio; the
  `#B4691C` → `#B4531C` proximity reason recorded in a comment; `.admin-main`
  transform push.
- `frontend/index.html`, `pos.html`, `admin.html`, `reports.html`, `track.html`,
  `prep.html` — inline colour literals moved onto tokens; `.app-version` markers
  (via `bump-version.mjs`).
- `frontend/js/*.js` (17 files: `admin*.js`, `app.js`, `changelog.js`, `pos*.js`,
  `reports.js`, `track.js`) — inline `style="…"` colour literals and
  `var(--tok,#hex)` fallbacks moved onto defined tokens; the pre-order and receipt
  state markup gained its word and shape channels.
- `frontend/manifest.json` — `theme_color` `#6B4226` → `#3A2A1F`,
  `background_color` `#FDFBF7` → `#FFFFFF`. **Colour values only. The JSON syntax
  error was NOT repaired — see (ae).**
- `frontend/sw.js` — `CACHE_NAME` → `rlc-cafe-v1.77.0` (via `bump-version.mjs`).
  **No `SHELL` additions: this release adds no new file.**
- `frontend/changelog.json` — four volunteer-facing entries, no hex values.

## Verification

Re-run from scratch for this release, not inherited:

| Check | Result |
|---|---|
| `node --check` on every `frontend/js/*.js` + `sw.js` | **clean**, 27 files + `sw.js` |
| `cd backend && npx tsc --noEmit` | **exit 0** |
| `npm test` (`TZ=UTC jest`) | **20 suites, 421 passed, 18 SKIPPED** |
| `npm run version:check` | **clean at v1.77.0**, all 6 markers, SHELL complete |
| `backend/` or `infra/` files changed | **none** |
| `frontend/changelog.json` parses | **yes** |
| Dangling `var()` references | **none** (see note) |
| Colour fallbacks `var(--tok,#hex)` remaining | **zero** |
| `.pos-`/`.board` rule bodies using `var(--brand)` | **zero** |
| Layout-property transitions in `frontend/css/` | **zero** |
| `tmp/` and `.impeccable/` staged | **no** — both gitignored, no untracked files |
| Credentials / PINs / tokens in the diff | **none** |

The **18 skips are `integration.test.ts` self-skipping** in the absence of
credentials. They are skips, not passes, and are reported as such. Per the brief,
`integration.test.ts` was **not** run with credentials or
`RUN_LIVE_WRITE_TESTS=1`, and **no Playwright journey was run** — so nothing in
this pass wrote to production.

**Dangling `var()` — corrected detail.** A first audit appeared to find seven
undefined properties; that was a false positive from a regex that only matched
definitions at the start of a line. Re-run properly: of 96 property names used,
**95 have a definition**. The one that does not, `--x`, occurs **only inside a
prose comment** at `style.css:3` and is not a real reference.
`--cl-progress` *is* defined — inline, in the `style=""` attribute
`pos-checklist.js:94` writes — and its `var(--cl-progress,0)` fallback is one of
only four remaining fallbacks, all of which are **non-colour**
(`--radius-lg,16px`; `--transition,all .15s`; `--cl-progress,0`; `--radius,8px`).

**Not verified in a browser by this pass.** All contrast, ΔE and hue-scan figures
above were measured in-browser during the design work; this release pass
re-verified them **statically only**. See the user checklist at the end.

## Follow-ups — new in this pass

- **(ae) `frontend/manifest.json` is invalid JSON and has NEVER parsed. High
  value, cheap — the best item on this list.** Three shortcuts read
  `"url": ./track.html",` — a **missing opening quote**. Confirmed directly:
  `JSON.parse` fails at line 27, and it fails **identically at `HEAD`**, so this
  is long-standing and not from this release. Consequence: the PWA has **no theme
  colour, no icons and no shortcuts** — the entire manifest is discarded by the
  browser.

  The colour values inside it are now correct (`theme_color:#3A2A1F`,
  `background_color:#FFFFFF`). The syntax was **deliberately left broken**,
  because repairing it would newly *activate* the whole manifest — install
  prompts, icons, an address-bar theme colour and three shortcuts all appearing
  for the first time. That is a **behavioural** change and it does not belong in
  an isolated repaint that must stay revertible. Fix it as its own small release
  and check what the shortcuts actually do before they become reachable.
- **(af) `display.html` / `display.css` (the TV board) and `prep.html` keep their
  dark navy world.** Deferred on purpose: a dark variant of the new palette is a
  **design decision the user has not reviewed**, and inventing one unreviewed is
  exactly what this release exists to avoid. They are internally consistent as
  they stand.
- **(ag) The 8-way `.pos-fam-*` walk-up palette is unreviewed.** It still contains
  **three cool tints**, inconsistent with the warm world. It also carries two
  **pre-existing body-text failures**: `.pos-fam-tea` price ink at **4.40:1** and
  `.pos-fam-water` at **4.17:1**, both under the 4.5:1 floor. Needs a review pass
  of its own, since eight mutually-distinguishable families is a real constraint.
- **(ah) Pill and stepper resting borders sit at 1.87:1**, below the WCAG 1.4.11
  3:1 floor for non-text UI — **consistently, by design**, across the set. Raising
  them is a visible density change on every screen, so it is a deliberate decision
  rather than a bug to patch quietly. Note it is a *known* non-conformance, not an
  oversight.

## Documentation and skills updated in this pass

- **`.kiro/skills/invariants/SKILL.md`** — new **`## Colour and contrast`**
  section with five checkable assertions, each carrying the production bug it
  prevents: token must be *defined* not fallback-supplied (261 drifted fallbacks,
  three fallback-only tokens); `--brand` off the POS board **with the ΔE 4.7 /
  9.9 figures**; a status needs a non-colour channel, citing the **ΔE 0.4**
  collision; the receipt indigo is shape-not-hue and the outline plus band are
  mandatory; measured floors for control borders and placeholders, and `opacity`
  on text as a contrast change. Plus: mock deviations are recorded with their
  measurement. Also **corrected two stale statements** in the same file — the
  motion invariant no longer claims `.pos-main` / `.admin-main` transition
  `margin-left` (they do not, as of this release), and the `[disabled]` invariant
  no longer describes the brand gradient as current. `frontend/css/style.css`
  `:root` added to the do-not-duplicate table as the single source of colour.
  Frontmatter `description` extended so a future agent touching colour, CSS
  custom properties, contrast or status badges actually loads the skill.
- **`.claude/skills/invariants/SKILL.md`** — pointer stub; `description`
  frontmatter re-synced to match the `.kiro` copy. Body unchanged.
- **`docs/feature-history.md`** — v1.77.0 sprint entry.
- **`docs/architecture.md`** — the PWA line claimed a manifest with icons and a
  theme colour. Corrected to record that it has never parsed (see (ae)).
- **`docs/update-20260817.md`** — this section.

Not updated, and why: `api-reference`, `db-schemas`, `order-lifecycle`,
`pricing-rules` and `test-suites` — this release adds no endpoint, no table or
attribute, no status transition or counter effect, no pricing rule and no test
suite or marker. `.kiro/steering/project.md` — no new `backend/src/routes/*.ts`,
`backend/src/lib/*.ts` or `frontend/js/*.js` module (all 25 files are
modifications; `git status` shows no untracked files). `release-flow` and
`release-checklist.md` — the release procedure itself did not change.
`docs/requirements.md` — nothing in it is contradicted.

## Still for the user to run — v1.77.0

Nothing was deployed, pushed or committed beyond the local commit on `master`.

```bash
npm run deploy:frontend    # git push; Actions publishes to GitHub Pages
```

**Frontend only.** No backend change, so `npm run deploy:backend` is not needed —
and note the asymmetry that has bitten before works *in our favour* here: this
commit touches `frontend/**`, so the Pages workflow will fire.

**Then check in a browser, because this pass ran none.** Use **Check for update**
in the footer to confirm you are on the `rlc-cafe-v1.77.0` shell, then:

1. **The POS board is the priority.** Sit a pre-order, a preparing order and a
   ready order side by side. They should be obviously different *and* each say
   what it is in words.
2. A **receipt-sent** card should have a 2px outline all the way round **and** the
   `RECEIPT SENT — check it` band. Both, not either.
3. Open and close the POS and admin sidebars. The push should be smooth on the
   counter iPad — that is the `margin-left` → `transform` change.
4. Tap into any text box on any page: the border should be clearly visible and the
   hint text inside it readable.
5. Glance at the admin dashboard charts — the three-segment trio should read as
   three categories, none of them looking like the brand orange.
6. **If anything looks wrong, `git revert` is clean** — that is the whole reason
   this shipped alone.
